# 0.0.0 — the language probes — DONE (2026-09-03)

> **Scope:** fourteen small Nitpick programs that ask the compiler whether the
> shapes `meta/specs/` depends on are spellable, compilable and correct. No
> library code is written in this subcycle. What it produces is **a verdict per
> probe** and, where a verdict is negative, an amended specification and a
> recorded decision.
>
> **Why first.** `meta/specs/` was written against the language's documentation
> and against a reading of the compiler's source, and the compiler project's
> most expensive finding is that *a construct that parses is not a construct
> that works* — most of its cycle 0.4 was repair, and every repair dated to the
> cycle that had parsed the construct. Two of this library's load-bearing
> shapes (RX-050's offsets-not-slices, and the explicit-stack parser) exist
> *because* of a language rule; the rest are unverified until asked.

---

## 1. Where you are

- Nothing is built. `src/` holds placeholder directories.
- The compiler is at `../../nitpick`, cycle 1.5 (verification), self-hosting
  declared under D-202. **Do not build it from here and do not read its `build/`**: the
  artifact these probes use is the pinned toolchain the board names
  (`../BOARD.md`, W-18) — `$NPKC` and `$NPKRT`, supplied by the orchestrator or
  set by hand from `../.internal/toolchain/<commit>/`.
- There is no `nregex` harness yet. **Probes are run by hand**, one command
  each, and that is deliberate: the harness is 0.0.2 and building it against an
  unverified set of language assumptions is the mistake this subcycle exists to
  avoid.
- The probes live in `tests/probe/` and **stay in the tree permanently**. They
  are a regression suite for the language shapes this library depends on, and
  0.0.2 picks them up as ordinary `program`-stage entries.

---

## 2. The command

For each probe, from the `nregex` tree root, with `$NPKC` and `$NPKRT` from the pinned
toolchain (the board names the commit, W-18):

```
$NPKC tests/probe/probeNN_name.npk -o /tmp/p.ll \
  && llc -O0 -filetype=obj -relocation-model=static /tmp/p.ll -o /tmp/p.o \
  && ld.lld -static /tmp/p.o $NPKRT -o /tmp/p \
  && /tmp/p ; echo "exit $?"
```

A probe that is **expected to be refused** stops at the first command, and its
verdict is the diagnostic code and span, recorded verbatim.

**Every probe exits 0 on success and a distinct positive code per assertion it
fails**, in the compiler's own house style, so a failure names itself.

**What `exit 0` additionally asserts, precisely.** **D-151 counts `wild`
blocks, D-188 counts live drivers, and neither sees a managed body.** So a probe
that takes `wild` bytes and exits 0 has proved they were released on that path,
and a probe whose only heap traffic is managed — a `string` body, a `Vec`'s
elements — has proved nothing about it. `nitpick-time` measured a `Vec<string>`
retaining **125 MiB over two million elements and still exiting 0** (RX-110).
Where a probe's obligation is managed, the gate is a memory cap and not an exit
code, and the real instrument is the compiler's `NPK_HEAP_STATS`, which does not
exist yet; when it lands, this becomes a `peak_live` assertion.

---

## 3. Decisions taken at planning

- **P-0 — a probe's filename may not begin with a digit**, so probes are named
  `probeNN_topic.npk` and the `mod:` line matches. A file's module name must
  equal its basename, and no identifier may begin with a decimal digit — D-147
  gives that opening to numeric literals alone, so `mod:01_argv;` is refused
  `NITPICK-LEX-003` with `NITPICK-RESOLVE-005` behind it. *Measured the hard
  way in `nitpick-posix`'s probe 02, 2026-09-03, after every plan in the
  ecosystem had written `01_name.npk`.*
- **P-1 — a probe is a program, never a library file.** It has its own `main`
  and `failsafe` and imports nothing from `src/`. A probe that needed library
  code would be testing the library, which is what every other cycle is for.
- **P-2 — a refused probe is a *result*, not a failure of the subcycle.** Two
  probes (06's second half, and 09 entirely) are **expected** to be refused,
  and their value is the exact diagnostic. The subcycle fails only if a probe's
  verdict is *undecided* — a crash in the compiler, a hang, or an output nobody
  can read.
- **P-3 — the verdict is recorded before the next probe is written.** Fourteen
  written and then run together is fourteen where the third one's answer would
  have changed the fourth one's design.
- **P-4 — a negative verdict amends `meta/specs/` in the same commit.** Not a
  note to self, not a TODO. The specification is the authority (RX-002) and an
  authority known to be wrong and not corrected is worse than none.
- **P-5 — the probe programs stay.** They become `tests/probe/`'s permanent
  suite, so a compiler change that breaks one of these shapes is caught by
  `nregex`'s own harness rather than by an engine failing mysteriously in 0.8.

---

## 4. The probes

Each entry: **what it asks**, **the shape**, **why it matters**, and **what a
negative verdict costs**.

### 01 — a large array of POD instruction values

**Asks:** does a `Vec<Inst>` of 100 000 twelve-byte structs fill, read, copy
and clear without trapping, and what does `#size_of<Inst>()` actually report?

```nitpick
struct:Inst = { InstKind:kind; uint32:a; uint32:b; };
```

**Matters because** `Program.insts` is exactly this (`COMPILE.md` C-1) and
`NREGEX_PROGRAM_INSTRUCTIONS` is 100 000. **Assert the size**: a padding
surprise changes the memory budget and the specification's number.

**A negative verdict costs** the program layout, and `COMPILE.md` §1's struct
is re-derived from what the compiler actually lays out.

### 02 — a tagged enum with payloads, in a `pick` and in a `Vec`

```nitpick
enum:HirKind = { Literal(uint32); Class(int32); Repeat(int32, int32); Empty; };
```

**Asks:** destructuring in a `pick`, storing in a `Vec<HirNode>`, comparing,
`#[derive(Eq, Debug)]`, and `#size_of`.

**Matters because** `HirKind`, `InstKind` and `PatternErrorKind` are all this
shape, and `HirNode` lives in an array.

**A negative verdict costs** the HIR and instruction representations:
parallel-field structs instead of payload variants, which is a worse API and a
larger node.

### 03 — `move T:v` into a generic container

**Asks:** does `vec_push`-shaped code work for `T = int32` and for `T` an
owning struct (one holding a `string`), including the container's drop?

**Matters because** `Vec<T>` (RX-006) must hold both: `Vec<Inst>` is POD,
`Vec<GroupInfo>` may own a name.

### 04 — an inherent impl on a generic struct with a mutating pointer receiver

```nitpick
impl:<T>:Vec<T> = {
    func:push2 = NIL(Vec<T>->:self, move T:v) never fails { … };
};
```

**Asks:** does D-171's inherent family impl work, called as `v.push2(x)`?
**Also asks the free-function form**, so the fallback is measured in the same
probe.

**Matters because** the whole of `src/core/` is this shape.

### 05 — an explicit-stack parser, and a refusal instead of a crash

**Asks:** a parser for a nested bracket language driven by an explicit
`Vec<Frame>` rather than by recursion; 250 levels deep succeeds; **10 000
levels deep produces a clean refusal, not a segfault.**

**Matters because** it is `SYNTAX.md` Y-9 and `SAFETY.md` S-18 — the rule that
a pattern's nesting cannot blow the call stack. This probe is the proof that
the explicit-stack approach is both writable and sufficient.

**A negative verdict** — if the explicit stack is awkward enough to be
unwritable — costs a language request, because the alternative (native
recursion with a depth counter) does not close the hazard: the counter is
checked *after* the frame is pushed.

### 06 — offsets, not slices — **the second half expected refused**

**Asks:** first, a function returning `struct:Match = { int64:lo; int64:hi; }`
built from a `uint8[]` parameter — expected **accepted**. Then a function
**returning the `uint8[]` subrange itself** — expected **refused**.

**Matters because** RX-050 is built on that refusal, and the whole API's shape
follows from it. If the second half is *accepted*, RX-050 is over-cautious, a
`Match` could carry a slice, and `API.md` §2 changes.

**Record the diagnostic code and span verbatim either way.**

### 07 — `string_bytes` at the borrow edges

**Asks:** `string_bytes(s)` into a scanner, the slice's `.len` and `.ptr`, and
the four edges the escape analysis governs — passed down (legal), returned
(refused), stored in a struct outliving the call (refused), held across an
`await` (refused).

**Matters because** `RX-054` makes `uint8[]` the primitive and every entry
point takes one. It also maps exactly which of the four the compiler actually
refuses, which is not the same question as which the specification says it
does.

### 08 — a sparse set over two `Vec<int32>`

**Asks:** O(1) insert, membership and clear over 100 000 keys, with the
invariant `dense[sparse[k]] == k` asserted after every operation, and the
uninitialised-`sparse` trick (reading garbage is safe because `dense` is
checked) exercised deliberately.

**Matters because** `SparseSet` is what makes the Pike VM linear
(`ENGINES.md` R-6) and the "read uninitialised memory safely" trick is exactly
the kind of thing a language with definite-assignment analysis (D-010) may
refuse. **If it refuses, the set needs a zeroed `sparse` array**, which costs
an `O(n)` clear and changes the Pike VM's per-byte cost — a real finding.

### 09 — a `comptime` pattern walker — **expected refused**

**Asks:** a `comptime func:` that indexes a pattern string byte by byte and
returns the position of the first `(`.

**Expected to be REFUSED**, and this probe exists to record *how*. Measured at
the compiler's 1.5.0 in `src/frontend/resolve_type.npk`: `fold_expr` has no arm
for an index expression, and `fold_string_builtin` handles exactly four names,
none of which reads a byte.

**Matters because** it is O-N1's evidence. A compile-time-validated pattern
literal — a malformed pattern becoming a *compile error* — is a property no
regex library in any language offers, and the gap is one `fold_expr` arm.

**The verdict to record** is the exact diagnostic: whether it is a clean
"cannot be evaluated at compile time" (`NITPICK-TYPE-025` or its neighbour) or
something less specific. A good diagnostic makes the request easy to argue.

### 10 — what `comptime` *can* do

**Asks:** the boundary from the other side — `string_concat`, `string_equals`,
`string_byte_length`, `string_is_empty`, a counted `loop`, a mutable local, a
nested `comptime func:` call, all inside `comptime(…)`.

**Matters because** a request is stronger when it says what already works. It
also tells us whether a *pattern-length* or *pattern-equality* check is
available at compile time even without indexing, which would be a small but
real win for a future `#regex_len_check` form.

### 11 — a bitset array field

**Asks:** `uint64[4]` as a field of a struct held in a `Vec`, with union,
intersection, complement, `contains` and iteration; and `#size_of` asserted at
32 bytes.

**Matters because** `ByteSet` is this, and a byte class lives inside an
instruction operand table.

### 12 — the prelude `Iterator` trait on a borrowing struct

**Asks:** implementing `Iterator` (`assoc:Item; func:next = Item?(Self->:self);`)
on a struct holding a `Regex->`-shaped borrow, and driving it with `for … in`.

**Matters because** O-A1 depends on the answer: whether `Matches` can implement
the trait at all, given the struct is second-class.

**A negative verdict costs** nothing structural — `matches_next` is the
explicit form and always works — but it settles O-A1 immediately.

### 13 — the verification rungs refuse by name

**Asks:** `prove(x > 0i32);`, a `limit<Rules>` parameter, and a
`requires`/`ensures` contract, each expected to be refused with
`NITPICK-RUNG-001` naming "1.5".

**Matters because** `VERIFICATION.md` P-1 writes every obligation as a comment
in the syntax it will take, and that is only safe if the real syntax is known
to be *inert* — refused loudly rather than parsed as something else. Confirmed
in the source at `src/backend/ir/ir_stmt.npk`; this probe confirms it in
practice.

### 14 — a program at the size bound

**Asks:** a `Vec<Inst>` built to `NREGEX_PROGRAM_INSTRUCTIONS` (100 000),
walked once, freed, exiting 0.

**Matters because** it is the memory and time floor of the whole library, and
because `exit 0` asserts through D-151 that the `wild` block was released on
every path. That assertion is exact here and covers the whole obligation **only
because `Inst` is POD**: no field of it owns heap storage, so the block is all
there is to release. The same probe over a `Vec` of an owning element type would
exit 0 with its elements orphaned (RX-110).

---

## 5. The steps

Each step is one commit.

**Step 1 — the shape probes: 01, 02, 06, 07.** These decide the program and HIR
representations and the whole API's shape. Run them, record verdicts, and if
06's second half is *accepted*, **stop and re-plan** `API.md` §2 before writing
another probe.

**Step 2 — the algorithm probes: 05, 08, 11, 14.** These decide whether the
parser and the Pike VM are writable as specified. 08's uninitialised-`sparse`
question is the one most likely to produce a real finding.

**Step 3 — the dispatch and generic probes: 03, 04, 12.**

**Step 4 — the compile-time probes: 09, 10, 13.** Expected refusals, run last
because their verdicts change nothing about what gets built — only what gets
asked of the compiler.

**Step 5 — the record and the amendments.** Every verdict written into §7;
every consequence written into `meta/specs/` and `meta/DECISIONS.md`;
`tests/probe/README.md` written, saying what the directory is for and that a
probe is never deleted.

---

## 6. What the executor should know

- **The reserved words will bite immediately.** `range`, `end`, `in`, `limit`,
  `any`, `buffer`, `raw`, `move`, `error`, `on` — `specs/BUILD.md` §7 is the
  list. A probe about a bitset wants a local called `range`; a probe about a
  match wants `end`. Neither is available; use `rng` and `hi`.
- **`discard(x);` takes parentheses. `defer { … }` takes no trailing
  semicolon.** Declarations end `};`; control-flow blocks do not.
- **Adjacent string literals do not concatenate.**
- **A file's `mod:` name must equal its basename**, or the loader reports
  `NITPICK-RESOLVE-005` at line 1 and says nothing about the name.
- **`raw` is licensed only on a `never fails` callee** (D-163). An `async`
  callee is never `never fails`, so `raw await f()` is unspellable.
- **Strings are move-only** (TYPE-046). Build a name per use rather than
  holding one binding across lines.
- **A `failsafe` must name every reachable error** (REACH-002), including the
  system ones the program's own machinery can raise. Copy the arm set from
  `../../nitpick/tests/backend/programs/proc_tool.npk` and prune.
- **Exit 0 means "and no `wild` block was left live"** — that and no more.
  **D-151 counts `wild` blocks, D-188 counts live drivers, and neither sees a
  managed body** (RX-110). Every probe exits 0 on success on purpose, and for a
  probe whose heap traffic is managed that exit is silent rather than clean.
- **`llc` and `ld.lld` must be the pinned 20.1.2.** `llvm-config --version`.

---

## 7. Verdicts

*(One row per probe, filled as they run. `ACCEPTED` / `REFUSED <code> at
<span>` / `UNDECIDED <why>`, plus the measured facts where a probe measures
something.)*

| # | Probe | Verdict | Measured | Consequence |
|---|---|---|---|---|
| 01 | POD instruction array | **ACCEPTED** | `#size_of<InstKind>` = **4**, `#size_of<Inst>` = **12** | none — `COMPILE.md` C-1's twelve bytes confirmed |
| 02 | payload enum | **ACCEPTED**, incl. the multi-field `Repeat(int32,int32)` | `#size_of<HirKind>` = **12**, `#size_of<HirNode>` = **24** | the payload spelling costs the widest variant on every variant; `HIR.md` H-2's parallel-field node keeps 3 operands in 20 bytes |
| 02b | `#[derive(Eq)]` on it | **REFUSED** `NITPICK-TYPE-034` at `<derived-1>:2:82` | — | workbench O-N10, **not new**; comparison written by hand |
| 02c | `#[derive(Ord)]` on it | **ACCEPTED and WRONG** — `Repeat(2,5).cmp(Repeat(9,9))` is `Equal` | arity makes no difference | workbench O-N10; probe is green while the compiler is wrong and turns **red** when it lands |
| 03 | generic `move` | **ACCEPTED** at `int32` and at an owning `T` | `#size_of<string>` = **24**, `OwningGroup` = **32** | freeing the block does not drop elements; `HIR.md` H-2's offset-into-`Bytes` design avoids it entirely |
| 04 | inherent generic impl | **ACCEPTED** — *and* the free-function form | both work, at two instantiations | the container API is a **choice**; cycle 0.0.4 settles it with both measured |
| 05 | explicit-stack parser | **ACCEPTED** | 250 parses; 251 refused at byte 250; **10 000 refused at depth 251** and the program survives | `SAFETY.md` S-18 confirmed writable and sufficient |
| 06a | returning offsets | **ACCEPTED** | `#size_of<Match>` = **16** | RX-050's return shape confirmed. An `Optional` is **not** `pick`-able — `== NIL` and `??` |
| 06b | returning a slice — *expected refused* | **ACCEPTED — HYPOTHESIS REFUTED** | compiles and runs correctly | **RX-050 stands.** The acceptance is workbench O-N9 (compiler DEF-3), not a licence. §5's "stop and re-plan `API.md` §2" is **not** followed, and RX-112 says why |
| 07 | `string_bytes` borrow edges | edge 1 (down) **legal**; edges 2–3 are O-N9, **not re-run**; edge 4 (`await`) **accepted** | `.len` and `.ptr` both readable | the `@`-borrow **control also passes** at edge 4, so it is not an O-N9-style asymmetry and is **not** reported as one |
| 08 | sparse set | **ACCEPTED**; the uninitialised `sparse` read is legal | D-010 governs locals, not heap | the guard is mandatory — but see 08b for why |
| 08b | `wild T->` index out of range | **NOT CHECKED** — reads and returns, exit 0 | index 999 of 4 | **RX-111.** `SAFETY.md` §1's bounds row was wrong; §5.3 (S-23) added; the accessor pair is now the only bounds check |
| 08c | slice index out of range | **TRAPS**, exit 94 `OutOfBounds` | same offset, same shape | the contrast that proves 08b |
| 09 | `comptime` walker — *expected refused* | **REFUSED** `NITPICK-TYPE-004` | the wall is **`string_bytes`**, and `.len` is already past it | O-N1's evidence, and its ask is sharpened: two arms, not one |
| 10 | `comptime` capabilities | **ACCEPTED** — loops, mutable locals, and all four foldable string builtins | `string_concat`, `string_equals`, `string_byte_length`, `string_is_empty` all fold | a pattern **length** check is buildable today; a nested `comptime func:` chain must be `never fails` |
| 11 | bitset array field | **ACCEPTED** | `#size_of<ByteSet>` = **32** | `>>>` does not parse but costs nothing (`>>` on unsigned is logical, measured at bit 63); a `uint64` above 2^63−1 cannot be spelled |
| 12 | `Iterator` on a borrowing struct | **the impl is ACCEPTED** | — | settles **O-A1**, and splits it |
| 12b | `for … in` over it | **REFUSED** `NITPICK-BORROW-009` | — | the trait's ergonomic argument is void; the explicit `next` is the sole driver |
| 13a | `prove` | **REFUSED** `NITPICK-RUNG-001`, "enabled by cycle 1.5" | — | `VERIFICATION.md` P-1's comments are safe: the syntax is inert |
| 13b | `limit<Rules>` | **REFUSED** `NITPICK-RUNG-001`, naming 1.5 | — | and `never fails` + `limit` is a **permanent** refusal, `NITPICK-TYPE-037` — a bound rule needs an error channel |
| 13c | `requires` | **REFUSED** `NITPICK-RUNG-001`, naming 1.5 | — | as planned |
| 13d | `ensures` | **REFUSED** `NITPICK-RUNG-001`, naming 1.5 | — | one diagnostic covers both contract clauses |
| 14 | program at the size bound | **ACCEPTED** | 100 000 × 12 B = **1 200 000 B**; peak RSS **1152 KiB**; wall **< 10 ms** | the memory and time floor, measured rather than budgeted |

**Every row's evidence is [`../../../tests/probe/TRANSCRIPT.txt`](../../../tests/probe/TRANSCRIPT.txt)** — every
command with its exit code, committed verbatim. All 23 probes' outcomes equal
their `expect-` headers.

### The two hypotheses that were refuted

Recorded here rather than left in the rows, because a refuted hypothesis is the
most valuable thing a probe produces and the easiest to quietly tidy away.

1. **06b was planned as "expected refused" and is accepted** (RX-112). The plan
   instructed the executor to stop and re-plan `API.md` §2 on that outcome; that
   instruction predates O-N9's confirmation and is not followed.
2. **08's own header claimed the unguarded sparse-set read TRAPS**, and the
   probe written to prove it disproved it (RX-111). The truth is worse: it
   returns an unrelated heap word.

---

## 8. Acceptance

- [x] fourteen probes exist under `tests/probe/`, each with a header comment
      naming what it asks and which specification rule depends on the answer —
      **23 files**, because five questions split into a positive and a negative
      half that neither file could carry alone (`ls tests/probe/*.npk`)
- [x] every verdict recorded in §7, with the exact diagnostic where refused
- [x] every negative verdict's consequence landed in `meta/specs/` and recorded
      in `meta/DECISIONS.md`, **in the same commit as the verdict** — RX-110,
      RX-111, RX-112
- [x] probe 09's diagnostic quoted verbatim into `meta/OPEN_QUESTIONS.md` O-N1,
      **with the isolation table that sharpens the ask from one arm to two**
- [x] `tests/probe/README.md` written
- [x] no probe deleted, ever — they are 0.0.2's first `program`-stage entries.
      *One was renamed rather than deleted*: `probe08b_unguarded_traps.npk`
      became `probe08b_wild_index_unchecked.npk` when it refuted the claim it
      was written to prove, and its header records that history rather than
      hiding it
- [x] **every probe run through all four steps of §2**, with every exit code in
      `tests/probe/TRANSCRIPT.txt`, committed verbatim — because `npkc` exiting
      0 does not mean a program is well-formed (workbench O-N11)

---

## Execution record

### 2026-09-03, stream 1, toolchain `950bb1d` (LLVM 20.1.2)

**What was done.** Twenty-three probe programs under `tests/probe/`, each run
through all four steps of §2 — `npkc`, `llc`, `ld.lld`, and the binary. The plan
called for fourteen; five of the fourteen questions split into a positive and a
negative half that no single file could carry, because a program that is refused
cannot also assert, and a program that traps cannot assert after it traps.
The verdicts are §7. The evidence is
[`../../../tests/probe/TRANSCRIPT.txt`](../../../tests/probe/TRANSCRIPT.txt),
committed verbatim: every command, every exit code, plus the three isolation
tables that probe headers cite.

**Every probe's outcome equals its `expect-` header**, checked mechanically
against the transcript rather than by reading — 23 blocks, 23 files, no
mismatches.

**Six commits** — four for the four steps, plus two for the specification
corrections that could not wait for the end of a step (P-4: a negative verdict
amends `meta/specs/` in the same commit as the verdict).

> *Corrected 2026-09-03 by cycle 0.0.1 (RX-114), at the author's direction.*
> *This sentence said "Five commits, one per step plus two", which is neither
> the count nor the arithmetic: the same paragraph's `commits:` list has six
> entries and `git log cb84308^..9b80d69` returns six. A verifier caught it as a
> wording slip. It is corrected in place, and marked, because the correction
> moves this record toward its own evidence — everything else in this file is
> left exactly as it was verified at `9b80d69`, including its `O-N1` and `O-N4`
> citations, which mean this repository's legacy local ids and are redirected
> from `meta/OPEN_QUESTIONS.md` rather than rewritten here.*

---

#### What was found, in order of how much it changes

**1. `SAFETY.md` was wrong about bounds, and the truth is worse — RX-111.**
D-070's check attaches to types that carry a length. A `wild T->` block is a
bare pointer and indexing it is raw pointer arithmetic; `Vec<T>.items` is one.
So `Program.insts`, `Hir.nodes`, `Program.classes`, every engine's thread list
and the sparse set are **unchecked**, and an out-of-range program counter is a
silently wrong match rather than the crash §1 advertised.

*The route matters more than the finding.* Probe 08's own header asserted that
the classic sparse-set trick traps in Nitpick where it is merely undefined in C.
Probe 08b was written to **prove** that sentence and disproved it — exit 10
where 94 was expected. Two contrast probes now pin it: a slice index 999 of 4
exits 94, the same index through a `wild` block exits 0. **This is the third
time in this ecosystem that a probe written to confirm a specification sentence
has refuted it**, and it is the argument for writing the confirming probe even
when the sentence looks obvious.

**2. The plan's instruction on probe 06b is overridden — RX-112.** §5 told the
executor that an accepted slice return means "stop and re-plan `API.md` §2". It
is accepted, and the plan predates the answer: the acceptance is the workbench
registry's O-N9, the compiler's DEF-3, already scheduled. RX-050 stands.
Re-planning an API onto a rule known to be under-enforced and known to be
scheduled for repair is the workaround W-11 forbids — it would compile today and
break at the re-pin.

**3. The leak gate now says what it covers — RX-110.** Six sites, not the four
the dispatch named; the extra two came from `git grep` and would not have come
from reading. `SAFETY.md` §8b (S-22) carries the compiler's own formulation
verbatim and names which of this library's structures are POD and therefore
fully covered, with the `NPK_HEAP_STATS` hook left in.

**4. O-A1 splits.** The `Iterator` impl on a borrow-holding struct is legal;
`for … in` over it is `NITPICK-BORROW-009`. The question's *recommendation*
survives and its *reason* does not, which is recorded in
`meta/OPEN_QUESTIONS.md` for cycle 0.10 to settle.

**5. O-N1's ask is sharper.** The wall is `string_bytes`, not the index — `.len`
alone is already past it. Two `fold_expr` arms, not one. The isolation table is
in the question and in the transcript.

#### Language facts that cost time, and are not in any list this ecosystem keeps

- **`stack` is a reserved word** — a MemoryQualifier beside `wild`, `wildx` and
  `defer` (`LEXICAL_REFERENCE.md` line 52). It is the most natural name for the
  local an explicit-stack parser keeps, which is probe 05's whole subject. The
  failure reads as a brace imbalance fifty lines away, and cost the better part
  of an hour to bisect. **Not in `PLAYBOOK.md` §10, not in this repository's
  `CLAUDE.md`, not in `meta/specs/BUILD.md` §7.**
- **An `Optional` is not `pick`-able.** `pick (m) { (NIL) {…}, (Match:g) {…} }`
  is `NITPICK-PARSE-005`. `TYPE_REFERENCE.md` §11.1: an `Optional` has no
  readable members, and the two accessors are `== NIL` and `??`. This is
  caller-visible on every entry point in `API.md` §2, since `regex_find` returns
  `Match?`.
- **`%` and `/` each add two mandatory `failsafe` arms** — `DivByZero` and
  `DivOverflow` (REACH-002, D-179). A single `%` in probe 01 cost two arms. This
  is the error budget in §3's sense and it is charged by *arithmetic*, not by a
  declared `error:`.
- **An `async` program owes a `DeadlineExceeded` arm** as well.
- **`>>>` does not parse**, though `TYPE_REFERENCE.md`'s operator table lists it.
  It costs nothing: `>>` on an unsigned type is already logical, measured at
  bit 63 — `(1u64 << 63u64) >> 63u64` is 1.
- **A `uint64` above 2^63−1 cannot be spelled as a literal.** The envelope is
  64-bit *signed* (D-148, `NITPICK-LEX-004`). `PLAYBOOK.md` records this for
  `int64`'s minimum; it is the same envelope and it bites the unsigned maximum.
  `~0u64` is how a full byte class is built.
- **`string_repeat` is documented and does not exist** —
  `NITPICK-RESOLVE-002`. The builtins that are present are the ones
  `BUILTIN_REFERENCE.md` gives an ABI row to.
- **A `comptime func:` called from another `comptime` body must be `never
  fails`.** It returns `Result<T>` like everything else, so it needs an unwrap;
  `raw` is licensed only by a `never fails` callee (D-163). The forcing form
  `comptime(f())` at a value position needs neither, so this only appears one
  level in.
- **`never fails` and `limit<Rules>` are mutually exclusive** —
  `NITPICK-TYPE-037`, a *permanent* rule rather than a rung. Most of `src/core/`
  is `never fails` today, so this decides which functions can carry
  `VERIFICATION.md`'s bound obligations at 1.5.

#### What it cost

About nine hours of wall time across five commits. The single largest item was
the `stack` collision (roughly an hour, all of it bisecting a brace imbalance
that did not exist); the second was building the O-N9 conclusions correctly,
which needed a mid-task correction from the coordinator about how DEF-3's
checker actually draws the line. No probe needed more than a few tens of MiB;
the largest, probe 14, peaks at 1152 KiB.

#### A note for whoever writes 0.0.4

`0.0.4.md` §2's API table settles `Vec<T>` as free functions. **Probe 04 shows
the inherent-impl form works too**, at a mutating `Vec<T>->` receiver and at two
instantiations. That is now a choice with evidence behind it rather than an
inherited assumption, and either answer is defensible — but the free-function
form is the compiler's own (`src/frontend/list.npk`, D-209) and RX-006 already
cites that lineage.

---

### The REPORT block

```
REPORT nitpick-regex 0.0.0
status: DONE
stream: s1
model: claude-opus-5
toolchain: 950bb1d
commits:
  - cb84308 cycle 0.0.0: step 1 — the shape probes (01, 02, 06, 07)
  - af164c1 cycle 0.0.0: the leak gate says what it covers (RX-110)
  - 902cf6c cycle 0.0.0: bounds are not what SAFETY.md said they were (RX-111)
  - c64e201 cycle 0.0.0: step 2 — the algorithm probes (05, 14)
  - 63a6b32 cycle 0.0.0: step 3 — the dispatch and generic probes (03, 04, 12)
  - HEAD cycle 0.0.0: step 4 and the record — the compile-time probes, and the verdicts
harness: not run: no harness until 0.0.2
  4-step tests/probe/probe01_pod_inst_array.npk -> npkc 0, llc 0, ld 0, run 0
  npkc tests/probe/probe02b_derive_eq_refused.npk -> exit 1  (NITPICK-TYPE-034)
  4-step tests/probe/probe02c_derive_ord_tag_only.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe02_payload_enum.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe03_generic_move.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe04_inherent_generic_impl.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe05_explicit_stack.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe06a_offsets_returned.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe06b_subview_returned.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe07_string_bytes_edges.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe08b_wild_index_unchecked.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe08c_slice_index_traps.npk -> npkc 0, llc 0, ld 0, run 94
  4-step tests/probe/probe08_sparse_set.npk -> npkc 0, llc 0, ld 0, run 0
  npkc tests/probe/probe09_comptime_walker.npk -> exit 1  (NITPICK-TYPE-004)
  4-step tests/probe/probe10_comptime_capabilities.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe11_byteset_bitset.npk -> npkc 0, llc 0, ld 0, run 0
  npkc tests/probe/probe12b_for_over_borrow_refused.npk -> exit 1  (NITPICK-BORROW-009)
  4-step tests/probe/probe12_iterator_borrowing.npk -> npkc 0, llc 0, ld 0, run 0
  npkc tests/probe/probe13a_prove_refused.npk -> exit 1  (NITPICK-RUNG-001)
  npkc tests/probe/probe13b_limit_refused.npk -> exit 1  (NITPICK-RUNG-001)
  npkc tests/probe/probe13c_requires_refused.npk -> exit 1  (NITPICK-RUNG-001)
  npkc tests/probe/probe13d_ensures_refused.npk -> exit 1  (NITPICK-RUNG-001)
  4-step tests/probe/probe14_size_bound.npk -> npkc 0, llc 0, ld 0, run 0
check: clean
record: meta/roadmap/0.0/0.0.0.md — DONE (2026-09-03)
next: n/a
findings-for-playbook:
  - `stack` IS A RESERVED WORD — a MemoryQualifier beside `wild`, `wildx`, `defer` (LEXICAL_REFERENCE.md:52). Missing from §10's table. It is the natural name for an explicit-stack parser's local, and the failure reads as a brace imbalance fifty lines away.
  - An `Optional` is NOT `pick`-able. `pick (m) { (NIL) {…}, (T:g) {…} }` is NITPICK-PARSE-005. Test with `== NIL`, read with `??` (TYPE_REFERENCE §11.1: an Optional has no readable members).
  - `%` and `/` each add TWO mandatory failsafe arms, `DivByZero` and `DivOverflow`; an `async` program adds `DeadlineExceeded`. The error budget is charged by arithmetic, not only by declared `error:`.
  - D-070's bounds check attaches to types that CARRY A LENGTH. A `wild T->` block is unchecked — every ecosystem `Vec<T>` is `wild T->`, so no container access is bounds-checked unless the library checks it.
  - The literal envelope is 64-bit SIGNED, so a `uint64` above 2^63-1 cannot be spelled either — build with `~0u64`. The playbook records only the int64-minimum half.
  - `>>` on an unsigned type is already logical; `>>>` does not parse. `string_repeat` is documented and absent. (O-N12 provisional.)
  - A `comptime func:` called from another comptime body must be `never fails` — it returns Result, and `raw` is licensed only by a `never fails` callee (D-163). `comptime(f())` at a value position needs neither.
  - `never fails` and `limit<Rules>` are mutually exclusive, NITPICK-TYPE-037 — a permanent rule, not a rung. Decides which functions can carry verification obligations at 1.5.
  - A struct holding a borrow CAN implement `Iterator`, but `for … in` over it is NITPICK-BORROW-009. The trait and the `for` loop are two different questions.
open-questions-raised: O-N12 (provisional — next free number proposed, awaiting confirmation)
for-the-author:
  - O-N NUMBERING COLLIDES IN THIS REPOSITORY. `meta/OPEN_QUESTIONS.md` carries legacy LOCAL O-N1…O-N4 alongside the workbench registry's O-N9…O-N12, which now mean different things in one file. I added a warning block and defined the workbench ids so citations resolve, but did not renumber settled entries. Recommend renumbering the four legacy ids to a local prefix (a local `O-C` prefix, "for the compiler") and reserving `O-N` for the registry alone.
  - `>>>` and `string_repeat` are documented and absent (O-N12, provisional). Recommend the documentation fix rather than the implementation: `>>` is already `lshr` on unsigned operands, so `>>>` would be a pure synonym.
  - `0.0.0.md` §5 told the executor to stop and re-plan `API.md` §2 if probe 06 accepted a slice return. It did; I did not, and RX-112 records why. Confirm that override.
compiler-defect: none
tokens-and-time: unknown — no harness reports either until 0.0.2. Wall clock ~9 h across six commits; largest single cost was the `stack` collision (~1 h bisecting a brace imbalance that did not exist). Peak memory of any probe: 1152 KiB (probe 14).
```
