# 0.0.5 — close — DONE (2026-09-06, second audit triage in §9; the CYCLE is not closed — READY-TO-CLOSE pending a third audit)

> **Scope:** reconcile what the cycle learned against what the specifications
> say, write 0.1's opening subcycle execution-grade, and archive.

## Steps

1. **The probe reconciliation.** Every verdict in `0.0.0.md` §7 re-read against
   `meta/specs/`. A verdict that forced a change should already have landed
   (0.0.0's P-4); this step is the check that it did, and it is done by reading
   the specification, not by remembering.
2. **O-G1's request.** Probe 09's diagnostic quoted verbatim into
   `meta/OPEN_QUESTIONS.md`, and the request written in the form it will be
   raised in the compiler repository. This is the cycle's one outward-facing
   product and it should not wait for 1.0.
3. **The findings.** Everything the cycle found that was not a probe verdict —
   a reserved word that cost a build, a harness shape that did not work, a
   language rule the specification had slightly wrong — written into this file
   as a numbered list. The compiler's execution records are load-bearing
   precisely because one writer kept them.
4. **The full run.** `harness/run.py` green, self-check first, every stage
   reporting, the verdict list committed.
5. **`0.1/0.1.0.md` written execution-grade**, before the cycle closes, so that
   0.1 is openable by a session that was not here.
6. **Archive.** `git mv meta/roadmap/0.0 meta/roadmap/done/0.0`, and
   `ROADMAP.md`'s row updated to point at the archive.

## Acceptance

- [x] every probe verdict reconciled against the specifications — **all 23 rows
      read against `meta/specs/` file by file. Six had a consequence that landed
      somewhere and not in the document that owns it; all six corrected,
      RX-135.**
- [x] O-G1's request written in raisable form, with the verbatim diagnostic —
      **and re-measured at `3d15ac9` first, which is how the stale file citation
      was caught.**
- [x] the cycle's findings written as a numbered list — §A below
- [x] a full green run, verdicts committed — **98/98 in 32.0 s**
- [x] `0.1/0.1.0.md` written
- [x] cycle archived, `ROADMAP.md` updated

---

## A. The findings — everything cycle 0.0 found that was not a probe verdict

Numbered so a sibling repository can act on one without reading the rest. Every
one was **measured here**, and the measurement is named. Where a finding
contradicts something this repository previously shipped, that is said rather
than quietly fixed.

### The language

**A1. `never fails` may carry `requires`, `ensures` and `limit`.** The compiler's
**D-241** retired D-163 rule 2's contract row on 2026-09-03, because a
`never fails` body already admits the trap channel. **This repository shipped
the opposite as a *permanent* `NITPICK-TYPE-037`** and wrote it into cycle
0.0.0's record as deciding which `src/core/` functions could carry an obligation
at 1.5 — while `VERIFICATION.md` §4's own P-2 example was `requires …
never fails`, the shape the claim forbade. RX-127.

**A2. `limit<Rules>` is live since the compiler's 1.5.2, and it charges every
consumer a `failsafe` arm.** A limited binding **anywhere in the reachable call
graph** makes `(LimitViolated)` mandatory in every consuming program — measured
with controls at **both** `pub` and module-private visibility, because
reachability follows the call graph and not visibility. The violation takes
D-241's trap route, so `?|`, `?!` and `is_err` at the call site cannot decline
it: an explicit `?| 55i32` over a violating call still exits **97**. This
library therefore declares no `limit` at all (S-24). RX-127.

**A3. A `/` or `%` ANYWHERE in a module arms `DivByZero` and `DivOverflow` in
EVERY importer.** Measured with a control: two test programs that call only
`bytes_init`, `bytes_push` and the accessor pair were refused
`NITPICK-REACH-002` for both arms **merely for importing** a `bytes.npk` whose
`bytes_put_uint` used `x / 10u64`. **Neither arm could ever have fired** — the
divisor was a literal and the operands unsigned — because the reachability walk
does not reason about values. **Any library promising a small `failsafe` bill
owes a sweep for division under `src/`.** RX-132.

**A4. A generic body is checked at EVERY instantiation (D-264), so a generic
operation that COPIES its element cannot exist at any `T`.** `vec_fill<T>(v, n,
x)` is `NITPICK-TYPE-046`: *"some of which own storage — a copy here would leave
two owners at those"*. **It is not a signature to fix.** `move` does not rescue
it, because the value is consumed by the first slot. The fill that works is
`vec_init_zeroed<T>` over `calloc` — the one that needs no value. RX-129's
neighbour, recorded at 0.0.4.

**A5. The 64-bit literal envelope is SIGNED.** `10^19` is `NITPICK-LEX-004`
*"outside the 64-bit literal envelope (D-148)"* **even as a `u64` that holds
1.8e19 comfortably**. The known rule — that `int64`'s minimum cannot be
spelled — has an upper-end twin nobody had written down, and a power-of-ten
table is exactly where you meet it. Build the value arithmetically. RX-132.

**A6. A reserved word is refused as a BINDING name and ACCEPTED as a struct
FIELD name.** `int64:end = 5i64;` is `NITPICK-PARSE-002`; `pub struct:Match = {
int64:start; int64:end; };` compiles, is built by struct literal, is read as
`m.end`, links and runs. Measured at **all three kept pins**, so this is not an
expiry — **the opposite claim was false the day it was written**, and it reached
seven sites over five subcycles. RX-134, and see B4 for why it survived.

**A7. `>>` is arithmetic on a signed operand and logical on an unsigned one, and
`>>>` does not parse** though the operator table lists it. Load-bearing here:
`byteset.npk`'s `b >> 6` and `b & 63` replace `/ 64` and `% 64` under A3, and
they are only exact because the operand is unsigned. 0.0.0 probe 11.

**A8. An `Optional` is not `pick`-able.** `pick (m) { (NIL) {…}, (Match:g) {…} }`
is `NITPICK-PARSE-005` — an `Optional` has no readable members. Test `== NIL`,
read with `??`. Caller-visible on most of `API.md` §1, because `regex_find`
returns `Match?`. 0.0.0 probe 06a.

**A9. `#[derive(Eq)]` and `#[derive(Ord)]` on a payload enum WORK and read every
payload field** — at `94874ce` and after. At `950bb1d` `Eq` was refused
`NITPICK-TYPE-034` and `Ord` compared **tags only**, so `Repeat(2,5).cmp(
Repeat(9,9))` answered `Equal`. **But `.eq()` returns `Result<bool>`**, so
`if (a.eq(b))` is `NITPICK-TYPE-007` — replacing a hand-written comparison with
a derive threads an error channel and charges the budget. RX-125.

### The instruments

**A10. `npkc` exit 0 does not mean a program is well-formed, and there is no
library object.** Every file in `src/` compiles at exit 0 and **every one is
refused by `llc`**, because a library file cannot define `@npk_failsafe` and
`npkc` never emits a `declare` for it. The library reaches the compiler only
through a program root. **Run all four steps — `npkc`, `llc`, `ld.lld`, the
binary — on anything you claim compiles.** RX-115.

**A11. `npkc`'s exit codes are an alphabet and `2` is not a refusal.** `0`
success; `1` refused **with diagnostics**; `2` the driver could not proceed and
**judged nothing**, with an empty stderr; `3` a trap. A test expecting 1 that
receives 2 was never compiled and proved nothing. **And a missing import exits
`1` with `NITPICK-RESOLVE-005` — the very code a rejection fixture expects** —
so a rejection test whose path is typo'd passes for the wrong reason, and only
code-set equality tells the two refusals apart.

**A12. `exit 0` proves no `wild` block was left live and proves nothing else.**
D-151 counts `wild` blocks, D-188 counts live drivers, **and neither sees a
managed body**. A `Vec` freed without dropping its owning elements exits 0.
**Where the obligation is managed the gate is a MEMORY CAP** — `vec_free_owning`
and `vec_free` under one 64 MiB cap must end **differently**, 0 and 92
(`HeapOom`). RX-110, RX-123, RX-126.

**A13. A low `ulimit -v` measures the loader, not the program**, so every
address-space bound needs a `/bin/true` control at the same cap. The capped
gate above runs that control on every capped file, and `mem-cap-mib:` is the
expectation header carrying it.

**A14. The prelude trim collapsed the empty-program floor from 29 undefined
symbols to 2.** D-262 emits a prelude item only if it is **referenced**, so
`SYMBOLS.txt` went 29 → 2 and `EDGES.txt` 237 → 2. **Any repository whose
no-syscall check is "equal to the baseline" now fails on the first program that
allocates** — a four-line program making one `wild` block already differs by
three symbols. The rule survives as a **reviewed residue list with a reason per
line, checked both ways**; the equality does not. RX-131.

**A15. RX-120's ecosystem-wide finding HAS EXPIRED, and the transcript proving
it is committed.** RX-120 said the undefined-symbol scan *cannot see a syscall* —
floor and syscaller both 29, difference empty, because `npk_sys6` was already
the prelude's. **At `3d15ac9` the floor is 2, the syscaller is 3, and the
difference is exactly `{npk_sys6}`.** Both pins are run back to back in
`harness/baseline/RX120.txt`. **RX-120's decision stands**: the call-edge scan
names the *calling function*, which no symbol set can at any pin, and it
survives a prelude that emits `npk_sys6` again. Four sibling repositories carry
the expired clause as current fact and each needs its own re-measurement.

**A16. Two of eleven residue entries were added by REASONING and the check
refused both.** `npk_chain_push` ("the `defer` pair travels together") and
`npk_int_to_string` ("interpolation must call the integer formatter") were
written from inference; the both-directions half of the check refused them
because **no scanned program references them**. That is a named-exemption list
caught at the moment of writing rather than three cycles later — and it happened
to the session that had just built the mechanism. RX-131.

**A17. `memset` is minted by `opt -O2` and absent at −O0.** The one residue
entry whose provenance is the optimiser rather than the source: `buffer_new`
zeroes and `calloc` zeroes, and at −O2 the optimiser recognises the loop and
emits a `memset` libcall. **An IR-shape claim measured only after optimisation
is a claim about the optimiser.** Take the number at both levels or say which
one you took.

**A18. The compiler's emission is INVOCATION-independent and
TREE-POSITION-dependent.** Four controls: three invocations sharing no working
directory and no argument form emit **byte-identical** IR; the same file copied
to a directory whose name is eight characters longer emits **24 more bytes**,
one per character per site row. D-236 renders every site path relative to the
**manifest root**. So *"an emitted `.ll` is path-dependent"* is true as an
observation and misleading as a rule — and the sharper statement is what makes a
cross-machine digest comparison legitimate. RX-133.

### The documents

**B1. A CHECK THAT DID NOT APPLY READS EXACTLY LIKE ONE THAT PASSED**, and this
cycle met it four times. The scans apply only to programs whose module graph
reaches `src/`; 19 of 25 probes import nothing from it. **The harness therefore
says, per run, how many units the scans ran on and how many they did not.** A
runner that merely prints a green line is what *"green because it never ran"*
looks like. RX-121.

**B2. `SAFETY.md` asserted two enforcements that did not exist**, one of them
guarding *the only bounds check this library has*. §5.3 said since 0.0.0 that
*"a tree check enforces that no `.items[` appears outside `src/core/vec.npk`"*;
`treecheck.ALL` held four checks and neither it nor its `.ptr[` twin. **A
consequence written in the document that DISCOVERED it, in a tense that reads as
discharged, and never carried to the document that OWNS it.** Each page reads
complete on its own and nothing mechanical compares them. `check_accessor_
confinement` is built, registered and seen to fail both ways. RX-136.

**B3. `TESTING.md`'s check table was short in BOTH directions at once** — it
omitted a check that exists (`check_no_division`) and one that a specification
said enforced a rule but did not (`check_accessor_confinement`). **A table wrong
in both directions is the clearest possible statement that nothing was comparing
it to the code.** RX-136.

**B4. AN UNMEASURED JUSTIFICATION ATTACHED TO A CORRECT DECISION IS
INVISIBLE.** `Match`'s fields are `lo`/`hi` — a good choice — justified by
*"`end` does not parse as a field name"*, which is false at all three pins. It
survived five subcycles and reached seven sites **because every check that could
fire is a check on the decision, and the decision was right.** Not `check_refs`,
not `check_specs_current`, not a green suite can see a true rule resting on a
false reason. **Only running it can.** RX-134.

**B5. The summary page is corrected last, and no citation sweep can reach it.**
`meta/specs/README.md`'s *"the language in one paragraph, for a reader arriving
from C"* still said an out-of-range index traps — the belief probe 08b refuted
on day one — five subcycles after every rule that owns the topic was fixed. It
**cites no decision and no rule**, so a citation grep finds nothing, and it is
the paragraph a newcomer reads first. RX-123 named this mechanism; RX-135 met it
again in the same repository.

**B6. Two sections of ONE file contradicted each other for a subcycle.**
`VERIFICATION.md` §2's rung table said *"LANDED, and §5 does NOT take it"* while
§5's Rule P-4 still read *"when 1.5.2 lands, these become `limit`ed"*. **Eighty
lines apart. Proximity is not review** — a rung table is read when you want to
know what the compiler supports, §5 when you want to know what this library
does, and nobody has both questions at once. RX-137.

**B7. A count in prose goes stale silently while the runner prints the right
one.** CI's header said *"67 declared units … a 39-file sweep … Four tree
checks"*; the tree was 98, 57 and five, and moved to six inside the subcycle
that corrected it. **The number that is CHECKED is the one the runner prints.**
Re-derive from a green run's summary rather than adding to a comment.

**B8. A measurement dated to "the pin" goes false with nobody editing it.**
Three sites said *"measured at the pin"*. The pin moved twice. **A moving name
is not a date** — name the commit. Found while sweeping A15, and it is why
`irscan.py`'s header now says `950bb1d` rather than "the pin".

**B9. A sweep list given to a worker was wrong in BOTH directions** — the
leak-gate list named four sites for this repository, **two were miscitations to
unrelated content and two already carried the correction inline**, so *none of
the four ever stated the claim*; meanwhile this repository's own three-way sweep
over 126 tracked files found **13 candidates, 12 correct or exempt, and ONE
GENUINE SITE THAT WAS ON NO LIST**. A list simultaneously stale and short is
worse than no list, because it is actioned. **Re-derive per repository; never
carry such a list forward by copying it.**

**B10. A self-check case that predicts its own decay does not announce it — but
it does not pass silently either, and the difference is worth stating exactly.**
See §C.

---

## B. The one genuine leak-gate site, named

The 0.0.4 sweep's single find, called out here because the board has since been
corrected to say such a list must be **re-derived per repository**, and this is
the evidence for that rule.

**The site: `meta/roadmap/done/0.0/0.0.4.md` §5, step 2** — this repository's own
subcycle plan, which read *"and a leak test that exits 0"* without saying what
that covers. **Confirmed fixed in the tree at this close**, by reading it:

> 2. `Vec<T>` with its suite: … and a leak test that exits 0 — **which proves
>    the `wild` BLOCK was released and nothing else** (`SAFETY.md` §8b, S-22).
>    The managed half is step 2's second gate and it is a memory cap.

**Why it matters more than its size.** It was in a *plan*, not a specification —
the class of document nobody sweeps, and the one an executor follows literally.
An executor reading the unqualified sentence would have written the `wild`-block
test, seen it pass, and stopped; the managed half is exactly the case that
*passes an `exit 0` gate while leaking 125 MiB*. **The four sites the dispatch
named were all already correct; the one that was not was not on the list.**

---

## C. Self-check case 9, and what a decaying expectation actually does

**The claim to be checked:** case 9 predicted in its own header, on 2026-09-04,
that `npk_ralloc` would become legitimate at 0.0.4; it did; and the case *"would
have gone GREEN rather than reporting that its premise had expired"*.

**Reproduced rather than reasoned about.** The old fixture was restored from
`e35ba16`, case 9's `must_say` was returned to `` `npk_ralloc` ``, and the
harness was run against today's tree — where `npk_ralloc` **is** a reviewed
residue entry.

```
FAIL  case 9: a program needing a floor symbol neither the baseline nor the residue list has
      the harness failed it (exit 1) but never said '`npk_ralloc`'. A non-zero exit
      alone is not enough: it would also be produced by the runner crashing for an
      unrelated reason, which is a passing case whose red is unreachable.
```

**So the case did NOT go green, and the mechanism that caught it is `must_say`.**
The inner run exited 1 — but *for an unrelated reason*: in a tiny case tree only
2 of the 11 residue entries are referenced, so the both-directions check reported
the other nine. **Without `must_say`, case 9 would have been satisfied by that
red and gone green on a failure that had nothing to do with it.**

**Three things follow, and only the third is a gap.**

1. **The prediction in the 0.0.4 report is too strong.** It was written from
   reasoning; the measurement says the case reddens either way — by `must_say`
   if the harness fails for the wrong reason, and by *"THE HARNESS PASSED IT"*
   if it does not fail at all.
2. **The reason it holds is a mechanism this repository already built**, and it
   should be stated as the general rule: **a self-check case asserts the words
   its red must contain, not merely that a red happened.** Every case here names
   its own fixture file for exactly this reason.
3. **The gap is the DIAGNOSIS, not the detection.** The red reads *"the harness
   failed it but never said X"* — which describes a **regression in the runner**,
   and sends a reader to debug the harness. What actually happened is that the
   **case's premise expired**. Those want different words.

**The cheap mechanism, proposed and deliberately NOT built here.** Give a `Case`
an optional `premise` — a callable returning `None` or a string — checked
*before* the case runs, so an expired premise reports `PREMISE EXPIRED: npk_ralloc
is now on the residue list` instead of a failure that looks like a broken
runner. Roughly ten lines. **It is not built in this subcycle** because the
self-check is the instrument the cycle's gate rests on, this is the close, and
the detection it would add is **zero** — the case already reddens. It buys a
better sentence, and that is a 0.1 item, not a 0.0 one.

**And case 9's own fix at 0.0.4 is the better answer anyway, because it is
structural**: the fixture now leans on `npk_mono_now`, chosen to be **unaddable**
rather than merely unused — D-076 and this ecosystem's determinism rule mean no
clock may ever join the residue list — **so the premise cannot expire twice.**
Prefer an expectation whose premise cannot decay over a mechanism that reports
decay.

---

## D. What cycle 0.0 taught

*Written at the close, for a reader who was not here and cannot ask anything.
This section and §A are the cycle's most durable output — the compiler's
equivalents are the reason its later cycles cost less than its earlier ones.*

**1. A probe that fails is the cheapest thing this cycle produced, and a probe
that REFUTES ITS OWN HYPOTHESIS is the most valuable.** Two did. `probe06b` was
planned as *expected refused* and was **accepted** — the plan's instruction to
stop and re-plan `API.md` §2 was correctly **not followed**, because the
acceptance is a compiler defect (O-N9) rather than a licence. `probe08b`'s own
header claimed the unguarded read *traps*, and the probe written to prove it
**disproved it**: it returns an unrelated heap word. That second one rewrote
`SAFETY.md` §1, added §5.3, and made an accessor pair load-bearing —
**a whole safety rule that existed because somebody ran the program instead of
believing the table.** Rename the probe when its hypothesis dies, and keep the
history in its header; do not tidy it away.

**2. The specification is the authority, and it is also the document most likely
to be stale.** Both are true at once and the second does not weaken the first.
Six of 23 verdicts had a consequence that landed in an open question, a record
or `CLAUDE.md` and **not in the rule that owns the topic**. The mechanism is
always the same: *the discovering document is not the owning document*, and each
one reads complete by itself. **Budget a reconciliation step at every cycle
close and do it by reading, not by grepping** — every one of these six was a
claim about tense or truth, containing no wrong token.

**3. Measure the extent, not just the existence.** A defect's *existence* is one
measurement and its *extent* is a different one, needing its own controls. RX-127
took three: `pub`, module-private, and an explicit fallback. RX-132 took a
control that never calls the function containing the division. **Understating an
extent fails silently and overstating one costs a message.**

**4. Prefer a check that fails to a rule that asks for care — and then check that
the check applies.** This cycle wrote five failing tree checks and a self-check
that runs first and proves the runner can fail eight ways. It also found, four
separate times, that **a check which did not apply is byte-identical to one that
passed**. So: print the denominator, print how many units a scan skipped and
why, and give any predicate that *gates* a check a positive and a negative case.

**5. A committed transcript is worth more than the sentence it supports, and the
gap shows up exactly when the claim travels.** RX-120's original measurement had
a table; its *expiry* was asserted in three places with no transcript — and the
expiry was the half being propagated to four sibling repositories. **A claim
other repositories will act on is reproducible by running a file.**

**6. Name the commit, not "the pin".** Three sites said *"measured at the pin"*
and the pin moved twice underneath them. Two probe verdicts have already
reversed under re-pins (RX-125's derives, RX-127's `limit<Rules>`), and a third
claim expired (RX-120). **A measurement against a moving compiler is dated by its
commit or it is not dated**, and *"is this current behaviour or behaviour at a
superseded pin"* is a question every claim here must be able to answer.

**7. The error budget is charged by three things and only one of them is a
declared `error:`.** An `error:` (S-8), **arithmetic** — `%` and `/` each add
two arms — and a **contract clause** — a `limit<Rules>` adds one. **A budget
audit that counts only `error:` declarations is counting one of three**, and the
other two are invisible in the API. This is the finding most likely to bite a
sibling, because every library here promises a small `failsafe` bill.

**8. Reachability is import-scoped, so a module's contents are its importers'
problem.** A division in `bytes.npk` charged programs that never called the
function. A limited binding charges consumers at module-private visibility.
**Module decomposition is part of the API**, not an implementation detail.

**9. `exit 0` is a narrower gate than four different sentences in this
repository's own plan claimed.** It sees `wild` blocks and live drivers and
**nothing else**. Every one of those four sentences was written by someone who
knew the rule. **Where the obligation is managed, the gate is a memory cap** —
and it needs a `/bin/true` control, because a low cap measures the loader.

**10. The mechanical warnings are not things to remember, and this cycle proved
it by paying them anyway.** `npkc` writes IR to stdout; `$?` after a pipeline is
the filter's status. Four sessions in this ecosystem violated the second
*having just read the warning*. **The fix is structural: pair every captured
status with the artefact it should have produced** — a refusal that wrote no
`.ll` reporting exit 0 is falsified immediately, and every violation so far was
caught that way and none by recall.

**11. What went right, and it is worth naming because it is repeatable.** The
order — probes, then harness, then primitives, then close — meant every language
surprise was met by a ten-line program instead of by a half-built engine. The
self-check existed **before** the suite it judges. `src/core/` was written
against a runner that had already been shown able to fail. **Nothing in this
cycle was blocked on a question**, which is what the cycle map promised, and
that held.

**12. What cycle 0.1 should expect.** It is the first cycle that writes real
algorithmic code, so the three constraints that cost the most here move from
theoretical to daily: **no `/` or `%` under `src/`** (a `{n,m}` parser wants
one), **no native recursion** (a parser wants it more than anything), and **the
accessor pair** (an arena walker will reach for `.items[`). All three now have
a check that fails. That is the difference between cycle 0.0 and cycle 0.1.

---

## Execution record

**2026-09-06, stream 1, pinned toolchain `3d15ac9` under LLVM 20.1.2.** The six
planned steps, plus five additions carried in the dispatch. The reconciliation
was the expensive step and produced the most.

### 1. What was done

| | |
|---|---|
| **Step 1** | all 23 verdicts re-read against `meta/specs/` by opening the files. Six stale consequences found and corrected — RX-135, RX-136, RX-137 |
| **Step 2** | O-G1 **re-measured at `3d15ac9`** and written in raisable form. The diagnostic is byte-identical to `950bb1d`'s; `fold_string_builtin` still handles four names; `fold_expr` still has no index arm; **the file was renamed** — `resolve_type.npk` → `type_resolve.npk` — and three live sites cited the old path |
| **Step 3** | §A, 25 numbered findings in three groups |
| **Step 4** | `harness/run.py` **98/98 green in 32.0 s**, run four times across this subcycle |
| **Step 5** | `meta/roadmap/0.1/0.1.0.md`, execution-grade |
| **Step 6** | `git mv meta/roadmap/0.0 meta/roadmap/done/0.0`; `ROADMAP.md` updated |
| **Add. 1** | CI prints `sha256sum` and byte count for every artefact the pinned compiler's build leaves. **RX-133 measured why the comparison is legitimate** rather than assuming it |
| **Add. 2** | `harness/selfcheck.py`'s stale docstring corrected — **and a sweep found five more sites, where the dispatch named one** |
| **Add. 3** | `harness/baseline/RX120.txt` — both pins run back to back, every command with its exit code |
| **Add. 4** | the one genuine leak-gate site confirmed fixed and named — §B |
| **Add. 5** | case 9 reproduced; the premise-decay claim **corrected by measurement** — §C |

### 2. Three things measured that changed a conclusion

**(a) The dispatch's premise about case 9 was too strong**, and reproducing it
said so. The case reddens either way; what is missing is a *diagnosis*, not a
detection. §C. **A prediction about an instrument is worth reproducing even when
it comes from the session that built the instrument.**

**(b) A struct field named `end` compiles at all three kept pins.** The
reconciliation flagged `SAFETY.md`'s `Match` declaration as unparseable; running
it showed the declaration is fine and **this repository's own reason for the
`lo`/`hi` names is false**. The flag was right about the line and wrong about
why — *a challenge can be worth making and still be wrong.* RX-134.

**(c) The emission is invocation-independent.** The playbook's *"an emitted
`.ll`'s byte count is path-dependent"* reads as *"cannot be compared across
machines"*, which would have made addition 1 pointless. Four controls plus
D-236's own source say the dependence is on **tree position**, not invocation —
so the compiler side's cross-machine request is answerable. RX-133.

### 3. The sweeps, with their denominators

Per W-28, a sweep states its denominator and its exemptions together.

| Sweep | Denominator | Result |
|---|---|---|
| the RX-120 expiry claim | **145 tracked files**, 2 phrasings, case-insensitive | ~45 candidate lines; **6 stated it as current fact and were corrected**; the rest are correct, or are closed records and settled decision text (exempt under W-28) |
| the renamed compiler file | 145 tracked files | 5 sites; 3 live and corrected, 1 a closed record, 1 my own new text |
| the `end`-does-not-parse claim | 145 tracked files, 2 phrasings | 7 sites; 5 live and corrected, 1 settled decision text (superseded, not edited), 1 a probe header (corrected in place with the original left legible) |
| absolute paths outside `check_refs`' reach | **83 of 145** tracked files | **0 hits.** A latent gap, not a live leak — see the report |

**The first sweep is the one to learn from: the dispatch named one site and
there were six.** Two were in code — `irscan.py`'s failure *message*, which a
future reader meets only when the check fires, and case 8's `why` string, which
prints on every run. **Neither was on any list**, and neither was reachable from
the `RX-120` token alone in a way that distinguished a citation from a claim.

### 4. The new tree check, seen to fail

`check_accessor_confinement` — `SAFETY.md` S-23, built because the specification
said it existed (RX-136).

| control | result |
|---|---|
| clean tree | **0 failures**, 13 files, 2 confined accessors |
| `src/core/core.npk`, naming both patterns **in comments** | **not reported** — prose blanked |
| `.items[` added to `src/hir/hir.npk` | **failed**, `file:line:col` |
| `src/core/vec.npk` stops containing `.items[` | **failed**, the other direction |

`src/` was restored exactly after both mutations — `git status --porcelain src/`
empty.

### 5. The two mechanical warnings

`npkc`'s emission was redirected on every invocation. **No status was captured
through a pipeline**: every `npkc`, `llc`, `ld.lld` and binary run in this
subcycle has `echo "exit=$?"` immediately after it, on its own line, and
`RX120.txt` pairs each one with the artefact it should have produced.

### 6. REPORT

```
REPORT nitpick-regex 0.0.5
status: READY-TO-CLOSE
stream: s1
model: claude-opus-5
toolchain: 3d15ac9
commits:
  - HEAD cycle 0.0.5: cycle 0.0 reconciled against its specifications, archived, and 0.1 opened
harness: NPKC=... NPKRT=... python3 harness/run.py -> 98/98 unit(s) passed in 32.0 s.
check: clean — check_refs.py clean over 65 markdown files. check_record.py reports
  ONE finding and it is deliberate: 0.0's README carries an UNTICKED box, "cycle
  archived to `done/0.0/`, `ROADMAP.md` updated". It is unticked because the cycle
  is not closed (RX-140), it is ticked at the close an audit accepts, and a
  checker saying "this cycle has an open item" is reporting the truth rather than
  a defect. It is neither ticked nor struck: struck would mean "will not be done".
record: meta/roadmap/done/0.0/0.0.5.md — DONE (2026-09-06)
next: meta/roadmap/0.1/0.1.0.md
findings-for-playbook:
  - AN UNMEASURED JUSTIFICATION ATTACHED TO A CORRECT DECISION IS INVISIBLE, and nothing
    in this ecosystem can see one. `Match`'s fields are `lo`/`hi` -- right -- justified by
    "`end` does not parse as a field name", which is FALSE at all three kept pins: a
    reserved word is refused as a BINDING name and accepted as a struct FIELD name. It
    survived five subcycles and reached seven sites because every check that could fire is
    a check on the decision, and the decision was right. Only running it can find these.
  - THE SUMMARY PAGE IS CORRECTED LAST AND NO CITATION SWEEP CAN REACH IT. meta/specs/
    README.md's "the language in one paragraph" still said an out-of-range index traps,
    five subcycles after every rule owning the topic was fixed. It cites no decision and
    no rule, so a citation grep finds nothing -- and it is the paragraph a newcomer reads
    first. Every library here has such a page.
  - TWO SECTIONS OF ONE FILE CONTRADICTED EACH OTHER FOR A SUBCYCLE, EIGHTY LINES APART.
    VERIFICATION.md §2 said "LANDED, and §5 does NOT take it" while §5's numbered rule P-4
    still said "when 1.5.2 lands, these become limited". Proximity is not review: the two
    sections answer different questions and nobody has both at once.
  - A DIRECTORY MOVE SEPARATES MARKDOWN LINKS FROM PATH MENTIONS, AND check_refs SEES ONLY
    THE FIRST. Archiving cycle 0.0 broke 6 markdown links (caught) and stranded 41 path
    MENTIONS across 15 files (not caught, gate green) -- they are inline code spans, or
    live in .npk/.toml/.txt files. 83 of this repository's 145 tracked files are outside
    check_refs' denominator entirely. Every repository here will archive a cycle.
  - THE COMPILER'S EMISSION IS INVOCATION-INDEPENDENT AND TREE-POSITION-DEPENDENT, so the
    playbook's "an emitted .ll's byte count is path-dependent; quote the object" is right
    about the observation and misleading as a rule -- read plainly it says a cross-machine
    emission comparison cannot work, and it can. Four controls: three invocations sharing
    no working directory and no argument form emit byte-identical IR; the same file copied
    to a directory eight characters longer emits 24 more bytes. D-236 renders every site
    path relative to the MANIFEST ROOT. This is what makes the compiler's cross-machine
    request answerable at all.
  - RX-120 HAS EXPIRED AND THE PROOF IS NOW A COMMITTED FILE THAT RUNS BOTH PINS. floor 2 /
    syscaller 3 / difference exactly {npk_sys6} at 3d15ac9, against 29 / 29 / empty at
    950bb1d, same two sources, same two commands. Four siblings carry the expired clause as
    current fact. RX-120's DECISION stands: the call-edge scan names the calling function,
    which no symbol set can at any pin.
  - A MEASUREMENT DATED TO "THE PIN" GOES FALSE WITH NOBODY EDITING IT. Three sites here
    said "measured at the pin"; the pin moved twice. A moving name is not a date. Two probe
    verdicts have already reversed under re-pins and a third claim expired -- name the
    commit.
  - THE ERROR BUDGET IS CHARGED BY THREE THINGS AND ONLY ONE IS A DECLARED `error:` -- an
    `error:`, ARITHMETIC (`/` and `%` each add two arms to every importer), and a CONTRACT
    CLAUSE (a `limit<Rules>` adds one, at module-private visibility as well as pub). A
    budget audit counting only `error:` declarations counts one of three, and the other two
    are invisible in the API.
  - A SELF-CHECK CASE WHOSE PREMISE EXPIRES DOES NOT PASS SILENTLY -- REPRODUCED, and the
    0.0.4 report's prediction that it "would have gone green" is too strong. It reddens
    either way. What it CANNOT do is say WHY: the message reads "the harness failed it but
    never said X", which sends a reader to debug the runner when the world moved. The
    general rule worth carrying: a self-check case asserts THE WORDS its red must contain,
    not merely that a red happened -- without that, case 9 would have been satisfied by an
    unrelated red. Better still, pick an expectation whose premise CANNOT decay.
open-questions-raised: none
for-the-author:
  - check_refs.py's absolute-path/credential check covers 62 of this repository's 145
    tracked files -- markdown only. Measured: 83 non-markdown tracked files, 0 hits today,
    so the gap is latent rather than live. It was reachable in ordinary work this subcycle:
    the RX120.txt transcript carried two absolute toolchain paths and was sanitised
    deliberately, not because anything objected. Recommendation: extend the leak scan (not
    the link check) to every tracked text file; it is a one-line change to the enumeration
    and it is the half of the check with a security shape.
  - O-G1'S REQUEST IS READY TO RAISE and is self-contained in meta/OPEN_QUESTIONS.md under
    "O-G1, re-verified at 3d15ac9 and written in raisable form" -- quotable as it stands.
    Re-measuring first was not ceremony: it caught that the compiler file everything cites,
    src/frontend/resolve_type.npk, HAS BEEN RENAMED to src/frontend/type_resolve.npk. A
    request naming a path the maintainer cannot open costs a round trip before it is read.
    The other three claims (diagnostic text, four foldable builtins, no index arm) are
    byte-for-byte unchanged at the pin.
  - THE CI DIGEST STEP IS IN AND PRINTS RATHER THAN ASSERTS, as instructed. Two things the
    comparison needs. (1) The artefact is `.internal/quickemit/npkc.ll`, NOT `build/npkc.ll`
    -- this workflow runs quickemit.py, not `npkg build`; same source, same snapshot
    builder, same entry file, different output directory, and the output directory does not
    enter the IR. If they differ, THAT is the finding and the candidate causes are named in
    the workflow. (2) RX-133 establishes why the comparison is legitimate at all, against a
    playbook rule that reads as saying it is not.
  - THE ACCESSOR CONFINEMENT CHECK DID NOT EXIST AND SAFETY.md SAID IT DID, for five
    subcycles, guarding the rule S-23 calls "the only bounds check this library has". Built
    and seen to fail both ways. Worth a sibling sweep: the failure shape is a consequence
    written in the document that DISCOVERED it, in a tense that reads as discharged, never
    carried to the document that OWNS it -- and TESTING.md's check table was wrong in BOTH
    directions at once, which is the clearest possible sign nothing compares it to code.
  - THIS IS READY-TO-CLOSE, NOT DONE. §8's remaining gate is the audit triage. The archive
    move and ROADMAP.md's row are done because the dispatch named them as step 6; if the
    audit wants anything undone, it is one `git mv` back. meta/audits/ does not exist yet
    and this will be its first report.
compiler-defect: none
tokens-and-time: harness 98/98 in 32.0 s, run five times across the subcycle; one commit;
  no retries, no stress reds; 5 decisions (RX-133 … RX-137); 1 new tree check
```

### 7. Addendum, 2026-09-06 — one commit landed after the REPORT block above

**The block in §6 is not edited (W-28): it records what was true when it was
committed, at `e99edac`.** Its `commits:` list names that commit as HEAD, and
that is no longer HEAD.

| | |
|---|---|
| this commit | cycle 0.1's subcycle table now **links** `0.1.0.md` rather than naming it in plain text. The file was written by this subcycle and the table had no link because, until this subcycle, there was no file — so the row was correct when written and became a dead end the moment it stopped being. **The cycle's gate is "0.1 openable by a fresh session"**, and a session arriving through `ROADMAP.md` → `0.1/README.md` → the subcycle table is the path that gate describes |

**Why this is an addendum rather than a second REPORT.** A REPORT block is
evidence rather than documentation: correcting it in place would destroy the
record of what was believed when it was written, and there is no version of a
`commits:` list that stays correct after the commit carrying it. This is the
pattern `0.0.4.md` §8 set, for the same reason.

**And the gap itself is the cycle's own lesson arriving one more time.** Nothing
was broken: `check_refs` was clean both times, because plain text is not a
broken link — it is *no* link. The row said the right thing and led nowhere,
which is B5's shape (*the summary is corrected last*) in the navigation rather
than in the prose.

---

### 8. The W-22 audit triage, 2026-09-06 — **15 findings, 15 lines**

**The close reported in §6 was REFUSED.** `meta/audits/nitpick-regex-0.0-2026-09-06.md`
(copied into this repository at `meta/audits/`, because a repository must carry
the record it was judged by) returned **DO-NOT-ACCEPT** on **two blocking
findings, both in `src/core/`** — the cycle's headline deliverable. It was
preceded by an independent `npk:verifier` PASS on eight checks. That is the
second time an independent PASS has stood over a use-after-free here, and the
third use-after-free this repository has shipped under a green suite.

**§6's REPORT block is not edited (W-28) and neither is §7's addendum.** They
record what was believed when they were committed. Everything below is the
answer beneath them.

**THE DENOMINATOR, STATED BECAUSE A TRIAGE THAT DOES NOT COUNT IS THE DEFECT
THIS REPOSITORY KEEPS FINDING: 2 blocking + 4 adjudications + 9 non-blocking =
15, and all 15 carry a line.** Twelve fixed, two settled with no action and the
reason recorded, one deferred to a named owner. Counted by reading the audit
end to end and ticking, not by trusting this sentence.

#### The two blocking findings — both FIXED, both measured before and after

| | finding | before | after |
|---|---|---|---|
| **BL-1** | `bytes_take_string` returned a **borrowed view** while three documents called it owning | read-back after a growth **exit 46**; as a byte, **exit 170 = `0xAA`** (D-183 poison) | `bytes_copy_string`, one allocation and a copy — **exit 0**, twice over, after two growths |
| **BL-2** | `vec_reserve` **does not terminate** on `cap == 0`, `vec_free`'s own poison postcondition | **exit 124** under `timeout 6` | **exit 94**, `OutOfBounds`, this library's own stop |

**BL-1 — RX-138.** The route taken is (i), the copy, and not (ii), the honest
view. The deciding reason is that the house rule *a view is a PARAMETER, never a
return value* is **written in `bytes.npk`'s own header** and was violated eleven
lines below it; keeping the hazard and documenting it would have made the rule
advisory in the one file that states it. The copy is `string_concat("", view)`
rather than `string_slice(view, 0, len)` because `string_slice` returns a
`Result` and unwrapping it threads an error path that can never fire — the exact
shape RX-132 spent a subcycle deleting. **The three false sentences are gone**:
`0.0/README.md`'s checklist line, `harness/baseline/RESIDUE.txt`'s reason for
`npk_string_from_bytes`, and the function's own comment. And the test is the
point: `bytes_unit.npk` now **reads `out` back after the growth**, which returns
46 against the shipped file and 0 against the fixed one — measured both ways
before it was committed.

**A claim in the audit that the fix does NOT rescue, and it is worth more than
the fix.** The audit's probe C showed the view could not be returned from the
frame owning the `Bytes`. Neither can the copy — and the control proves it is
the **signature** and not the body: a function taking `Bytes->` and returning
`string_concat("small", "!")`, which cannot alias its argument at all, is refused
identically, while the same expression written INLINE compiles and runs. So
*"the only shape that may leave the frame"* is **deleted rather than repaired**.
Raised as **O-N17**, which is O-N9's exact inverse: O-N9 is a view escaping with
no diagnostic, this is an owned value refused a diagnosis it does not deserve,
and both are one tracker reading types where it needs values.

**BL-2 — RX-139, and the audit was right about the hang and overstated the other
two.** Measured: `vec_push` and `vec_insert` on a freed `Vec` exit **91**,
`HeapBadRequest`, because the runtime refuses `ralloc(p, 0)` outright (D-150).
They do not reach the write the audit describes. Real, deterministic, and less
severe than filed — and still wrong, because they stop on the ALLOCATOR's check
of a size rather than on this library's check of its own invariant.

**The fix is a trap and NOT the floor the audit suggested first**, and this is
the part a later session should not re-litigate. Copying `bytes_reserve`'s
`if (nc < 1i64) { nc = 1i64; }` would have handed `ralloc` a **dangling pointer
with a plausible size** — a loud hang traded for silent heap corruption.
`Bytes` has no free and allocates a fresh `buffer_new`, so the same guard in the
two files would have been two different decisions. *"The sibling has it and this
does not"* is a reason to look, never a reason to copy.

**AND THE EXTENT WAS ESTABLISHED RATHER THAN THE SITE FIXED.** Sweeping the
file for the same class found `vec_init_zeroed(-5)`, **which the audit did not
report and which is the worst of the four**: it floored the ALLOCATION to one
element and wrote `-5` into `count`, so `vec_push`'s `v.count >= v.cap` read
`-5 >= 1`, was FALSE, and the push wrote at `items[-5]` — five elements before
the block, **at exit 0**. Every other entry point already trapped on a negative;
the missing half is the one where **the negative goes into a FIELD rather than
into an index**, which no amount of checking indices would have found. That it
exists is the strongest available argument that this file has not been
exhausted, and it is why this pass reports DONE rather than READY-TO-CLOSE.

Five unit programs added, **each seen to fail before it was trusted**:
`vec_oob_reserve_freed` (124 → 94), `vec_oob_push_freed` (91 → 94),
`vec_oob_insert_freed` (91 → 94), `vec_oob_init_zeroed_negative` (0 → 94),
`bytes_oob_get_after_clear`.

**And the committed suite was run against the planted defect, not just the
probe.** Putting the borrowed view back under the new name and running the
harness reddens `bytes_unit.npk` at **exit 46, expected 0** — the whole point,
since that file was green over this defect for a cycle. *One correction to this
paragraph's own first draft:* the filtered run printed one `ok` and one `FAIL`
for the same path and it was read here as an optimisation-level divergence — a
use-after-free visible at one level and not the other, which would have been a
finding. **It is not.** The two lines are two different SUITES matching one
filter, the `parse` sweep and the `unit` suite, and building the planted file by
hand at both levels gives **exit 46 at −O0 and exit 46 through `opt -O2`**. A
guess about a suspicious output survived one sentence, which is the right number.

#### The four adjudications

**(a) RX-120's obligation — NOT discharged, now DISCHARGED by an executable.**
The audit's finding is worse than non-executability and it is upheld in full:
`RX120.txt:115` recorded `harness.irscan.kernel_call_edges`, **which exists
nowhere in this tree**, with `exit=0` and output belonging to some other,
unshown command. The conclusion was sound and the evidence was invented, in the
section being propagated to four sibling repositories, and a VERIFIED PASS did
not catch it. Two smaller faults pointed the same way: a decisive `diff`
consuming two `.undef` files no shown command created, and a line showing
`npkc -o X` while annotating it as a stdout redirect.

`harness/baseline/rx120.sh` replaces it. It **creates every intermediate it
later consumes** and **asserts** floor == 2, syscaller == 3, difference ==
`{npk_sys6}` and `npk_sys6` **not** in the floor at `3d15ac9`; with `950bb1d`
present it also asserts 29/29/identical with `npk_sys6` **in** the old floor,
which is RX-120 as originally measured. Both legs ran green here. It is a
harness **build step** and its own CI step, so a re-pin that moves the prelude
reddens a run. **Shown to fail before it was trusted**: four planted faults —
floor 2→3, syscaller 3→9, the difference name, historical 29→28 — each exit 1
naming the number that moved; a missing `npkc` exits **2** ("could not proceed
and judged nothing", `npkc`'s own alphabet) and a missing `950bb1d` prints
**SKIPPED** in capitals rather than passing. `RX120.txt` keeps its place as the
narrative with its fabrication **struck in place** and a preface saying so.

**And the instruction's intent, which is the transferable part.** The dispatch
that produced `RX120.txt` said to commit *"the actual command and its output"*.
A hand-copied transcript satisfies those words exactly and misses their purpose,
because **nothing can check it**. The file existed to stop a claim four
repositories would act on from resting on a sentence, and it replaced the
sentence with a sentence formatted like a terminal. **A claim is reproducible
when a FILE RUNS.**

**(b) The CI digest substitution — LEGITIMATE, and it stays a PRINT. RX-141.**
No action on legitimacy: `cmp`-identical at `05457db4…`, 21 514 197 bytes each.
**That comparison is SAME-MACHINE**, so it settles the substitution and not the
cross-machine claim, which has never been observed. The audit's suggestion that
the step could now assert is **declined with its cost stated**: asserting a
developer machine's digest turns an open measurement into a foregone one, and CI
would go red on the first genuine cross-machine difference and call it a broken
pin rather than the compiler defect it would be. Recorded in `BUILD.md` §8a and
in the workflow itself so a later session does not re-open it.

**(c) Coverage — FIXED, all three parts.** Truncation read-backs added:
`vec_unit.npk` reads element 0 back after `vec_truncate` and after
`vec_clear`+refill with **different** values (it asserted `count` alone, which a
truncate that also corrupted element 0 would pass); `bytes_unit.npk` reads all
four bytes back after a clear-and-refill, with `bytes_oob_get_after_clear.npk`
for the trapping half. **N-8's constant is gone**: the million-byte growth loop
pushed `88u8` for a whole cycle, so any mis-copy preserving length was invisible;
it now pushes `(k & 255) =>! uint8` and checks five indices with five different
expected values. `sparseset_unit.npk` was the model, as the audit said.

**(d) The error budget — FIXED.** The arithmetic holds at one arm and the two
dead arms are gone. The comment justifying them said `bytes_put_uint` USES `/`
AND `%`; `src/core/bytes.npk:186` says the opposite in capitals and `SAFETY.md`
S-25 records the rewrite, so **the justification was false about the file the
program tests**. What replaces them says the shape rather than the fact: the
defect was not a charge that appeared, it was **a charge removed from the code
and left standing in the consumer**, and a budget audit counting `error:`
declarations counts one of three ways this language charges an arm.

#### The nine non-blocking findings — seven fixed now, one moot, one deferred

| | finding | disposition |
|---|---|---|
| **N-1** | `TESTING.md` §8's check table: 13 rows, 7 exist, no column saying which | **FIXED** — a `Built` column, each unbuilt row naming its owning cycle, and V-19 rewritten in the future tense because it elevated two checks that had never been written |
| **N-2** | `CLAUDE.md` says six tree checks on line 19 and four on line 241 | **FIXED**, and there is now **one** statement of each count in that file. Embarrassing to defer: it is the file every session is told to read first |
| **N-3** | the archive note's counts match no state the tree has had | **MOOT** — RX-140 reverses the archive, which removes the note's subject. Its lesson is kept: *a count written in the present tense about a tree mid-edit is stale before the commit lands* |
| **N-4** | `API.md:164` calls `bytes_new()`, which has never existed | **FIXED** — `raw bytes_init(64i64)`, with a note on why nothing compared the specification's **examples** to the code that had just landed |
| **N-5** | `vec_unit.npk:19` cites `vec_owning_cap.npk`, which has never existed | **FIXED** — names the real pair, and says why `check_refs` could not see it (it reads markdown links in markdown files; a citation inside a `.npk` is checked by nothing but a reader) |
| **N-6** | `SAFETY.md` S-23's wording is broader than the check that enforces it | **FIXED** — the rule now says "anywhere under `src/`", which is the check's actual scope; the 84 occurrences across 20 files outside it were re-verified benign |
| **N-7** | the `check_refs` denominator moved inside the reporting subcycle (62/145 → 63/147) | **DEFERRED, to the workbench, with an owner.** The numbers are re-derived below. The **recommendation** — extend the leak scan to every tracked text file — is a change to `skills/check/scripts/check_refs.py`, which lives in the workbench and is read-only from here (W-16). It is in `for-the-author:` |
| **N-8** | `Bytes`'s growth test pushes the constant `88u8` | **FIXED** — see (c) |
| **N-9** | 39 lines across 20 files date a measurement to "the pin" | **FIXED, AND MADE A RULE — RX-142.** 17 live sites dated to `3d15ac9`, and `check_dated_measurements` registered as the seventh tree check so there is no third sweep. Shown to fail on two planted violations, the second proving the exemption marker is per **line** and not per file |

**N-7's numbers, re-derived at this commit rather than carried — and re-derived
AFTER the last edit, which is N-3's lesson applied to this very paragraph:**
`check_refs` reports **clean over 65 markdown files**; the tree has **155**
tracked files, 65 markdown and 90 not. (The audit found 63 and 147 at the
previous commit, against a REPORT block claiming 62 and 145 — the denominator
moved *inside* the subcycle that reported it.) The audit's observation stands
and is why its recommendation is worth acting on: the gate's denominator is
markdown links in markdown files, so **90 of 155 tracked files are outside it
entirely** — exactly the population a whole-directory move, or an absolute path
in a `.npk`, lives in.

#### The archive — REVERSED. RX-140.

`ROADMAP.md` said DONE and archived and the cycle is not. One `git mv` puts
`0.0/` back, so the 33 citations of `meta/roadmap/0.0/X` are correct again and
need no redirect; the handful of live pointers rewritten to `done/` were
rewritten back, and `0.0.0.md`/`0.0.1.md`'s link depth went from `../../../../`
to `../../../` — **the depth changed and no claim did**, in both directions.
`0.0.5.md`'s REPORT block and its two statements that step 6 *performed* the
move are untouched: **live navigation is corrected, historical claims are not.**
The reasons are in RX-140, and the shortest is that `done/README.md`'s rule that
archived notes are never rewritten could not have survived two blocking fixes
being made to files sitting inside `done/`.

#### What this cost, and what remains

Harness **GREEN 108/108 in 35.0 s**, grown 98 → 108; seven tree checks, six that
can fail; `check_refs` clean; `rx120.sh` green on both legs. Five decisions,
**RX-138 … RX-142**. One open question raised, **O-N17**.

**WHAT REMAINS IS ONE THING: AN AUDIT THAT HAS SEEN THIS TREE.** Cycle 0.0 is
not closed and `meta/roadmap/done/` stays empty. This pass reports **DONE** and
not `READY-TO-CLOSE`, deliberately, and the argument is not procedural: the last
close was reported ready, passed by an independent verifier on eight checks, and
was refused on two defects in one directory — and **this triage then found a
fifth defect in that same directory that the audit missed**, of the same class,
worse than two it did report. A session that has just fixed a file is the worst
available judge of whether that file is finished.

#### The REPORT block for the triage pass

**This is a SECOND block in this file and it does not replace §6's (W-28).**
§6 records what was believed at `e99edac`, including a `READY-TO-CLOSE` that was
refused; this one records the triage that answered it.

```
REPORT nitpick-regex 0.0.5
status: DONE
stream: s1
model: claude-opus-5
toolchain: 3d15ac9
commits:
  - 20bdcea cycle 0.0.5: cycle 0.1's subcycle table links the file this close wrote
  - e99edac cycle 0.0.5: cycle 0.0 reconciled against its specifications, archived, and 0.1 opened
  - HEAD cycle 0.0.5: the audit triage — a use-after-free, a hang, and an archive reversed
harness: NPKC=... NPKRT=... python3 harness/run.py -> 108/108 unit(s) passed in 35.4 s.
check: clean
record: meta/roadmap/0.0/0.0.5.md — DONE (2026-09-06, audit triage in §8; the CYCLE is not closed)
next: n/a — meta/roadmap/0.1/0.1.0.md already exists, written execution-grade at the
  refused close and unchanged by this pass except for two counts it quoted
findings-for-playbook:
  - A LEAK GATE IS NOT A CORRECTNESS GATE, and a suite that CONSTRUCTS a stale alias
    without reading it is green over a use-after-free. Three shipped here; two survived
    an independent VERIFIED PASS. The gate that works is a planted fault.
  - `string` DOES NOT ANNOUNCE THAT IT IS A VIEW and `uint8[]` does. "No view escapes"
    cannot be checked by reading types at a signature; check the `Views` column of every
    primitive a `pub` function returns the result of.
  - A SIBLING'S GUARD IS A REASON TO LOOK, NEVER A REASON TO COPY. `bytes_reserve`'s
    capacity floor is right for a managed buffer and would have turned `vec_reserve`'s
    hang into heap corruption, because the block there is already freed.
  - A HAND-COPIED TRANSCRIPT SATISFIES "commit the command and its output" AND CANNOT BE
    CHECKED. One here recorded a function that has never existed, with exit=0 beside it,
    and a VERIFIED PASS did not catch it. A claim is reproducible when a FILE RUNS.
  - A GREP IS A SWEEP; ONLY A CHECK IS A RULE. A phrase-level sweep closed 3 sites and
    left a class of 39. The third sweep is a tree check.
  - A COUNT WRITTEN IN THE PRESENT TENSE ABOUT A TREE MID-EDIT IS STALE BEFORE THE COMMIT
    LANDS. Re-derive after the last edit, or write no count.
  - A GATE WRITTEN AS A LIST OF GREEN THINGS IS SATISFIED BY A GREEN RUN. The clause that
    is not satisfiable by passing is an audit that has seen the tree.
open-questions-raised: O-N17
for-the-author:
  - `check_refs.py`'s denominator is markdown links in markdown files: 65 of this
    repository's 155 tracked files. The audit's N-7 recommends extending the leak scan to
    every tracked TEXT file, and the argument is concrete — the absolute-path and
    stale-pointer defects this ecosystem keeps finding live in `.npk`, `.toml` and `.txt`.
    The script is in the workbench and read-only from here (W-16). RECOMMEND: action it
    in the workbench, since it protects five repositories rather than one.
  - O-N17 is O-N9's inverse and I recommend raising the two together: the borrow tracker
    is type-shaped where it needs to be value-shaped, one fix closes an unsound accept and
    an over-strict reject, and if only one can be had it should be O-N9 — a false accept
    is a use-after-free and a false reject is an inconvenience.
compiler-defect: none
tokens-and-time: harness 108/108 in 35.4 s, run three times across the triage; one commit;
  no retries, no reds this session was unable to explain; 5 decisions (RX-138 … RX-142);
  1 new tree check; 5 new unit programs; 1 new executable assertion
```

---

### 9. The SECOND W-22 audit triage, 2026-09-06 — **6 items, 6 lines**

**The close was refused a SECOND time.**
`meta/audits/nitpick-regex-0.0-2026-09-06-second.md` returned **DO-NOT-ACCEPT**
on two blocking findings — and **one of them was introduced by the first
triage's own fix commit `58f5237`**, while the other, `BL-3`, is the largest
defect this cycle has found and had been sitting under a green suite since
`src/core/` was written.

**§6's REPORT block, §7's addendum and §8's triage are not edited (W-28).**
They record what was believed when they were committed. Everything below is the
answer beneath them.

**THE DENOMINATOR, AND IT HAS TWO HALVES BECAUSE THE TWO NUMBERS DIFFER FOR A
REASON.** The audit filed **5 findings**: 2 blocking (BL-3, BL-4) and 3
non-blocking (N-10, N-11, N-12). The dispatch split **BL-4 into (i) the false
comment and (ii) the missing memory-cap pair**, because its root cause was
raised as a compiler defect and confirmed as **DEF-25**, so the halves have
different owners. **That gives 6 items, and all 6 carry a line below.** Counted
by reading the audit end to end and ticking, not by trusting this sentence.
**All six are FIXED. None deferred, none refused.**

#### The six, one line each

| | item | disposition |
|---|---|---|
| **1** | **BL-3** — `vec_oob(0)` returns; nine entry points perform the access they refused | **FIXED — RX-143.** Index is now negative for every `i`; 12 new units, 10 of them seen to fail first; 3 false sentences corrected |
| **2** | **BL-4 (i)** — `bytes.npk`'s empty-path comment is wrong three ways (false claim, absent measurement, wrong instrument) — also **N-12** | **FIXED — RX-146.** All three named and corrected against the compiler's own source |
| **3** | **BL-4 (ii)** — no memory-cap pair for `Bytes`, and `exit 0` cannot gate it | **FIXED — RX-146.** The pair exists; the empty half is committed RED as `pending-until: fe42dba` |
| **4** | **N-10** — `check_dated_measurements` cannot reach the `.yml` its docstring names | **FIXED — RX-145.** Prunes by name; reports per-extension denominators; two `ci.yml` lines dated |
| **5** | **N-11** — the free paths report `Unreachable`, not `OutOfBounds` | **FIXED — RX-144.** Guard on `vec_free` and `vec_free_owning`; 3 units, all seen to fail at 95 first |
| **6** | **N-12** — the RX-138 comment cites a measurement absent from the tree | **FIXED — RX-146**, folded into item 2 as the audit directed, and carrying its own line here because a folded finding is the one that gets lost |

#### BL-3 — the fix, and why the tests matter more than the fix

**Reproduced independently before anything was changed**: `drop vec_oob(0)`
then `exit 50` gives **exit 50** at −O0 and through `opt -O2`. The controls at
1 and −1 give 94. The audit is right in every particular.

**The extent re-derived rather than inherited.** 28 `drop vec_oob(…)` call
sites at the shipped tree — counted here, and it **matches the audit's 28
exactly**. 30 now, the two additions being N-11's. `vec_oob` is the only
stop-like helper in the layer: every other `drop <call>` under `src/core/`
resolves to `vec_set`, `vec_free`, `bytes_reserve`, `drop_element`,
`bytes_push` or `bytes_extend`, none of which is a trap. And the sweep for
`never returns` / `does not return` / `ensures false` across every live file
returns only `vec_oob`'s own sites and RX-130's superseded text.

**The fix is `0i64 - 1i64 - (i & 1i64)` — negative, not merely non-zero.**
`guard[i | 1i64]` was measured working and **declined**: it is odd and therefore
never 0, which is correct only while the guard array is exactly one element
long, so widening `int64[1]` later would silently restore the defect. A negative
index is out of range for an array of any length. Verified at −1, 0, 1, 2, −2,
2⁶², and `int64`'s minimum and maximum, at both optimisation levels.

**AND THE HEADER'S STATED REASON FOR THE PARAMETER WAS ITSELF FALSE, WHICH
NOTHING ASKED ME TO CHECK.** `vec.npk` said `i` was passed *"only so the trap is
reached with a value the optimiser cannot prove constant"*. Measured: a
**literal** out-of-range index compiles at exit 0, `npkc` folds it to an
unconditional `call void @npk_trap(i32 -4099)`, and the program exits 94 at both
levels. Folding does not defeat this trap at `3d15ac9`. The parameter is kept
for the two reasons that are true and the false one is deleted. *A sentence
sitting two lines above a defect is worth reading as suspiciously as the
defect.*

**THE TESTS ARE THE POINT, AND THE ARITHMETIC IS THE ARGUMENT.** All twelve
shipped out-of-range units passed a non-zero argument — 3, 1, −1, 2, −1, 3, 16,
−1, −1, −1, −1, −5 — so **twelve of twelve avoided the one value at which the
stop did not stop**, and a thirteenth of the same shape would have too. So:

- **nine units at exactly the empty-container boundary**, one per broken entry
  point, each reproducing the shipped tree's own distinct exit code — 51, 62,
  60, 61, 53, 54, 55, and two new ones at 56 and 57;
- **three self-check units** asserting `vec_oob(k)` does not return for
  k ∈ {−1, 0, 1}. **This is the one that generalises**: it tests the primitive
  rather than an entry point, so it fails for the next spelling of the trap
  whatever that spelling is, and needs no extension when a tenth accessor is
  written. The −1 and 1 cases pass on both trees on purpose — they are the
  controls that would catch a "fix" that traps at 0 by ceasing to trap
  elsewhere.

**Ten of the twelve were seen to fail against a pristine `git archive` of HEAD
before they were trusted**, each with its own exit code, and all twelve pass at
both optimisation levels after. The `vec_remove` chain — `count == -1`, then
`vec_push` writing at `items[-1]`, then **exit 95** from `dalloc` finding the
heap header corrupted — was reproduced here too, because it is asserted in a
committed comment and a comment asserting somebody else's measurement is what
N-12 is about.

#### BL-4 — the split was already decided, and the sequencing trap was real

**No guard was written, and the guard is named in the tree so that nobody adds
one later.** `if (b.len == 0i64) { pass ""; }` would have made everything green
today. The defect was raised under W-11 instead, confirmed as **DEF-25**, and
fixed by the compiler side at **`fe42dba`** — *"nothing in library code needs a
guard."*

**(i)** The comment's three failures are corrected against the compiler's own
source, read with `git show 3d15ac9:runtime/npkrt.ll` rather than from a
document: `@npk_string_concat` has **no** empty short-circuit, while
`@npk_string_slice` **does** and says why in its own comment. That asymmetry is
what makes it a defect rather than a design, and RX-138's reason for preferring
`string_concat` — that `string_slice` returns a `Result` — reads differently
once the `Result` is understood as the price of the primitive that handles the
empty case correctly.

**(ii) THE SEQUENCING TRAP WAS HANDLED AND IT WAS NOT HYPOTHETICAL.** Measured
at our pin with the repository's own instrument, `/bin/true` passing under the
same cap: 8 000 000 calls under 64 MiB — **empty exits 92, non-empty exits 0**;
at 2 000 000 calls, peak RSS 62 592 KiB against 0. So the empty half is
**correct and red here**, and will stay red until the pin moves.

**The harness had no way to say that, so it does now.** `selfcheck.py` had a
pending mechanism and the unit runner did not. `pending-until: <commit>` is a
new marker (`BUILD.md` **B-5b**): the file is built, linked and **run**, its
actual exit printed, and counted as **neither a pass nor a failure** — P-18's
rule, *a pending case is not a passing case*.

**AND IT RETIRES ITSELF, WHICH IS THE HALF I WOULD DEFEND HARDEST.** If a
pending unit starts **meeting** its expectation the run goes **RED** and names
the action. That is the opposite of what a known-failure list normally does, and
it is deliberate: every recurring defect in this repository's audit history is a
rule that outlived its reason — N-6's over-wide scope, N-10's unreachable
directory, RX-131's expired measurement, RX-145's dead skip entries. A marker
that survives the day it stops being true is one more of them. **The run that
moves the pin is the run that says so.** Shown able to fail on three mutations:
a pending unit meeting its expectation reddens with `IS NOW STALE`; a blank
`pending-until:` is UNREADABLE rather than a silent skip; and a pending unit
that does not compile is an ordinary failure, because the marker excuses a wrong
exit code and not a refusal.

#### N-10 — reproduced, fixed, and the fix generalised

The shipped walk opens **132 files, `.yml` 0**, and reports `failures: []`,
while the check's own regex finds two matches in `.github/workflows/ci.yml`.
Reproduced here before the fix, not taken from the report.

Two changes. **Prune by NAME** — and the tell was already in the tree, exactly
as the audit said: `_UNDATED_SKIP_DIRS`' `.internal/` and `.git/` entries were
dead code the leading-dot prune had already removed, which is evidence the
author expected the skip list to be doing the skipping. It is now doing it, and
the two dead entries are gone. **And the check reports its denominator PER
DECLARED EXTENSION**, with a note saying a zero is a finding. `treecheck.py`'s
own module docstring has said since 0.0.3 that a check finding nothing because
it *looked nowhere* is indistinguishable from one that found nothing because
there was nothing to find — and then this check declared seven extensions,
opened zero of one for a whole subcycle, and reported one healthy aggregate.
**A class with a zero beside it is a question a reader can ask; a class absorbed
into a total is not.** Written up as `TESTING.md` rule **V-22**.

Shown to fail in the directory that was unreachable: a planted
`measured at the pin` in `ci.yml` is named at `file:line:col`; clean when
removed; and the shipped prune reaches **0** `.yml` files, which is the defect
stated as a number.

#### N-11 — fixed rather than documented, and the reason is in the file already

Measured here first: `vec_free` twice, `vec_free_owning` after `vec_free`, and
`sset_free` twice all exit **95, `Unreachable`**. Fixed rather than documented,
because `vec.npk`'s header makes **exactly this complaint** about `vec_push` and
`vec_insert` six paragraphs before claiming `cap <= 0` traps `OutOfBounds`
*"like every other misuse in this file"* — so documenting would have made that
paragraph advisory in the file that states it, which is the argument the first
triage used to fix BL-1 rather than document it.

`vec_free_owning` is the more important half and the less obvious: without the
guard it walks `0 .. count` **moving elements out of a freed block and dropping
them**. It is survivable today only because `vec_free` also zeroes `count` — and
`count` is a field a caller can write, since a `pub struct` has no private
fields (D-149).

**The transferable half is about the sentence.** *"Like every other X"* is a
claim about a set, and **the set was never enumerated**. It was written while
five entry points were being fixed and was false about the two nobody had looked
at.

#### What this cost, and what it did not fix

Harness **GREEN 141/141 in 46.2 s**, grown 108 → 141, **plus one PENDING that is
outside the denominator and is not a pass**. Seven tree checks; `check_refs`
clean **over 66 markdown files, leak scan 173 of 173 tracked** (65/155 before
this commit — the denominator moved inside the subcycle again, which is why it
is re-derived here after the last edit rather than carried). Four decisions,
**RX-143 … RX-146**. Seventeen new unit programs, one new expectation marker,
one new testing rule (V-22), one new specification rule (B-5b).

**The sweeps, with their denominators.** 30 `drop vec_oob(…)` call sites under
`src/core/` after the fix, **28 at the shipped tree — which re-derives the
audit's 28 exactly**, the two additions being N-11's. 46 functions declared
under `src/core/`; `vec_oob` is the only stop among them, every other
`drop <call>` resolving to `vec_set`, `vec_free`, `bytes_reserve`,
`drop_element`, `bytes_push` or `bytes_extend`. The `never returns` /
`does not return` / `ensures false` sweep over every live file returns only
`vec_oob`'s own sites and RX-130's superseded text. `check_dated_measurements`
now opens **133** files against the shipped walk's 132, the one difference being
the `.yml` it could not previously reach.

**What is NOT claimed.** The pin is still `3d15ac9`; the re-pin to `fe42dba` is
the board's to make, and one of the seventeen new units is red until it happens
— deliberately, visibly, and self-retiring. And no sweep here establishes that
`src/core/` is exhausted: the previous triage found a fifth defect the first
audit missed, this audit found a sixth the triage missed, and the honest
statement is that **each pass has found what the previous one's shape could not
see**, which is an argument for another pass and not for confidence.

#### The REPORT block for the second triage pass

**This is the THIRD block in this file and it replaces neither (W-28).** §6
records the refused close at `e99edac`; §8 records the first triage; this one
records the second.

```
REPORT nitpick-regex 0.0.5
status: READY-TO-CLOSE
stream: s1
model: claude-opus-5
toolchain: 3d15ac9
commits:
  - 20bdcea cycle 0.0.5: cycle 0.1's subcycle table links the file this close wrote
  - e99edac cycle 0.0.5: cycle 0.0 reconciled against its specifications, archived, and 0.1 opened
  - 58f5237 cycle 0.0.5: the audit triage — a use-after-free, a hang, and an archive reversed
  - HEAD cycle 0.0.5: the second audit triage — the out-of-range stop did not stop
harness: NPKC=... NPKRT=... python3 harness/run.py -> 141/141 unit(s) passed in 46.2 s.
check: clean
record: meta/roadmap/0.0/0.0.5.md — DONE (2026-09-06, second audit triage in §9;
  the CYCLE is not closed — READY-TO-CLOSE pending a third audit)
next: n/a — meta/roadmap/0.1/0.1.0.md exists, written execution-grade at the refused close
findings-for-playbook:
  - A GUARD'S STOP CAN FAIL TO STOP, AND NO AMOUNT OF SWEEPING FOR MISSING GUARDS
    FINDS IT. `vec_oob` was spelled as an index into a one-element array, so it
    returned for i == 0 — the one index nine `i >= count` guards pass when the
    container is empty. Test the STOP itself, not only the entry points that call it.
  - TWELVE BOUNDARY TESTS THAT ALL PASS A NON-ZERO ARGUMENT ARE ONE TEST. Count the
    ARGUMENTS a suite actually passes, not the cases it contains; the value that
    breaks is the one no case chose.
  - A TEST THAT IS CORRECT AND RED IS WORTH MORE THAN NO TEST AND FAR MORE THAN A
    WEAKENED ONE. When the defect is in the pinned compiler, commit the test red
    with the fixing commit named, and make the marker RED THE DAY IT PASSES so it
    cannot outlive its reason.
  - A CHECK IS A RULE ONLY OVER THE FILES IT OPENS. `check_dated_measurements`
    declared `.yml`, pruned dotted directories, and so could never open the tree's
    only `.yml`. Report the denominator PER DECLARED CLASS; a zero is a finding.
  - "LIKE EVERY OTHER X IN THIS FILE" IS A CLAIM ABOUT A SET NOBODY ENUMERATED. It
    was written while five entry points were fixed and was false of the two that
    were not looked at. Name the members or drop the quantifier.
  - THE SENTENCE ABOVE A DEFECT IS AS SUSPECT AS THE DEFECT. The comment explaining
    why `vec_oob` takes a parameter was itself false at the pin, and nothing
    required checking it — it was two lines from the bug.
  - A SUPERSEDED DECISION CANNOT BE TOLD FROM A LIVE ONE BY READING IT. RX-130 still
    reads "it never returns" with nothing on the page saying RX-143 replaced that
    sentence. The no-rewrite rule is right; the missing half is a forward pointer.
open-questions-raised: none
for-the-author:
  - `meta/DECISIONS.md` has 78 decisions and NO mechanism for marking one superseded.
    RX-130 reads as current and its central sentence is now false. The no-rewrite rule
    (D-085/D-202) is correct and should stay. RECOMMEND: a one-line `> SUPERSEDED IN
    PART BY RX-nnn` note above a superseded heading — which adds to the record rather
    than rewriting it — plus a `check_refs` rule that a decision naming another as
    superseded requires that note. It is a convention change across five repositories,
    so it belongs in the workbench and not in a triage.
  - THE RE-PIN TO `fe42dba` IS NOW LOAD-BEARING AND IT IS YOURS. DEF-25 is fixed there
    and `tests/unit/bytes_copy_string_empty.npk` is committed RED until this repository
    pins it. The unit reddens the run the moment the pin moves, which is the signal to
    delete its `pending-until:` marker in the same commit. RECOMMEND: re-pin, then
    delete the marker; do not delete the marker first.
compiler-defect: none — DEF-25 was raised under W-11 before this pass and is already
  fixed at `fe42dba`; nothing was worked around and no guard was written
tokens-and-time: harness 141/141 in 46.2 s, run twice in full plus four filtered mutation
  runs; 17 new unit programs, 10 of them seen to fail against a pristine archive of HEAD
  first; 4 decisions (RX-143 … RX-146); 1 new expectation marker shown to fail three ways;
  1 commit; no retries and no red this pass could not explain
```
