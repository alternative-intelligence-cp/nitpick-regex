# 0.0.1 — the skeleton — DONE (2026-09-03, amended twice on 2026-09-04)

> **Scope:** the tree `npkc` can be pointed at, the manifest's first test
> entries, a consumer program that imports the library and compiles, and CI. No
> functionality. **Gated on 0.0.0's verdicts being recorded**, because the
> module layout below assumes probes 01, 02 and 04 came back accepted.

## 1. What exists after this subcycle

```
src/lib.npk                 # the umbrella: `pub use` of the public surface
src/{core,syntax,hir,unicode,compile,engine,api}/<dir>.npk   # placeholders that parse
tests/conformance/import.npk
harness/                    # nothing yet — 0.0.2
.github/workflows/ci.yml
```

## 2. Decisions taken at planning

- **P-6 — one placeholder module per directory, and it parses.** An empty
  directory is invisible to the `parse` sweep, and the sweep is what stops the
  grammar being quietly made partial. Each placeholder is
  `src/<dir>/<dir>.npk` with `mod:<dir>;` and a header comment naming the
  specification section that will fill it and the cycle that does. They are
  replaced, not deleted.
- **P-7 — `src/lib.npk` re-exports deliberately, one line per public name.**
  `use` is not transitive (`MODULE_REFERENCE.md` §2.3), so the surface is a
  list a reviewer can read in one file. That is a feature and it is why the
  library has an umbrella at all. `API.md` §1 is the list it will grow into.
- **P-8 — the consumer test imports by relative path** (`BUILD.md` B-15) and
  carries a comment naming **O-G3** as the reason, so the day dependency roots
  work, the thing to change is greppable.
- **P-9 — CI pins the compiler by commit**, not by branch. The compiler is
  moving through cycle 1.5; a library whose CI tracks its `main` is a library
  whose CI breaks for reasons that are not its own. Bumping the pin is a
  deliberate commit that runs the full suite.

## 3. Steps

1. **The module skeleton.** Seven placeholder modules, `src/lib.npk`, and the
   layering comment in each header naming what it may import
   (`BUILD.md` §6's diagram). Commit.
2. **The consumer.** `tests/conformance/import.npk`: `mod:import;`,
   `use "../../src/lib.npk".*;`, a `main` that exits 0, a `failsafe` with the
   system arms **plus `ERegexPattern`** — which is the whole cost of importing
   this library and is worth seeing in the first test that exists. It compiles
   against an empty surface, which is the point: it keeps compiling as the
   surface grows, and the day it does not, the surface changed incompatibly.
3. **`nitpick.toml`.** The first `[[test]]` entries: `probe` (stage `program`,
   `tests/probe`) and `conformance` (stage `compile`, kind `positive`).
4. **CI.** A workflow that checks out `nregex`; checks out the compiler at the
   pinned commit; installs LLVM 20.1.2; builds the compiler; runs
   `harness/run.py` (a stub until 0.0.2, which still must exit 0). On push and
   on pull request.
5. **`CLAUDE.md` and `CONTRIBUTING.md` reviewed.** Both were written at
   repository setup, before the probes ran. This step re-reads them against
   0.0.0's verdicts and extends the reserved-word list and the "what will
   surprise you" section with whatever the probes actually cost.

## 4. Acceptance

- [x] `npkc src/lib.npk` succeeds and emits IR — **it does, and RX-115 is why that
      is not the check it looks like.** 421 KB of IR at exit 0, and `llc` refuses
      it: `use of undefined value '@npk_failsafe'`. The same is true of all seven
      placeholders. Keep the command — it parses and resolves the whole module
      graph — but **it is not evidence that the library builds**, and this line
      used to read as though it were.
- [x] `tests/conformance/import.npk` compiles, links, runs, exits 0 — **all four
      steps, and again through `opt -O2` with the same exit** (transcript §B, §C).
      This is the acceptance that carries the weight.
- [x] its `failsafe` names `ERegexPattern` and nothing else of ours — one arm, and
      the six system arms plus `(*)`. Control §E4 shows the compiler does **not**
      demand it while the surface is empty, so it is there as the contract; §E5
      and §E6 show `(*)` and the named arms discharging two different obligations.
- [x] every `src/` subdirectory has a parsing module — seven, plus the umbrella.
- [x] CI green on a push — **DONE 2026-09-04, and no longer struck.** Run
      [`33835762747`](https://github.com/alternative-intelligence-cp/nitpick-regex/actions/runs/33835762747),
      conclusion **`success`**, job `build` green, 8m48s, triggered by a `push` of
      `main` at **`c7b8711`** — this subcycle's own close commit, so what went
      green is exactly the tree 0.0.1 closed with. Read back off the run itself
      rather than taken from the report of it.
      **All ten declared steps passed** — sixteen step records once the runner's
      own setup, cache-post and teardown are counted, every one `success` — and
      the two that **could not be rehearsed from here** are among them: *Build the compiler from the pinned commit*, which
      ran through the compiler's own `quickemit.py` (its conclusion is `success`,
      not `skipped` — the cache was cold on a first run, so the build genuinely
      happened), and *Put LLVM on PATH and assert the pin*, which **asserts**
      20.1.2 and exits 1 on any other patch release rather than reporting it.

      **What the green proves, and what it does not.** It proves the pin is real:
      the compiler builds from a clean checkout on a machine that is not this one,
      with no prebuilt binary anywhere in the path — which is the whole thing W-18
      forbids rehearsing locally, and the reason this box could not be ticked on
      the day. It proves 0.0.1's one substantive acceptance reproduces there: the
      step *The conformance consumer, all four steps* compiles, links, **runs**
      `tests/conformance/import.npk` and judges the binary's exit code, which is
      why the run is not green over nothing.
      It proves **nothing about this library's behaviour**, because there is none
      yet. `harness/run.py` is still the toolchain-pin stub until 0.0.2, so **no
      test suite ran and no stage was exercised**; the manifest's `probe` entry is
      still commented out (RX-119). `ci.yml`'s header states the same boundary and
      is kept consistent with this line.
- [x] `CLAUDE.md` and `CONTRIBUTING.md` re-read against the verdicts and extended

---

## Execution record

### 2026-09-03, stream 1, toolchain `950bb1d` (LLVM 20.1.2)

**What was done.** Eight files in `src/` — seven placeholders, one per layer of
`BUILD.md` §6's diagram, and the umbrella — plus `tests/conformance/import.npk`,
the manifest's first `[[test]]` entry, `.github/workflows/ci.yml`, a harness
stub, and a re-read of both guides. Every command is in
[`../../../../tests/conformance/TRANSCRIPT.txt`](../../../../tests/conformance/TRANSCRIPT.txt)
with its exit code, including six controls.

**The subcycle produced almost no code and five specification corrections.**
That is the right ratio for a skeleton: what a skeleton tests is whether the
documents describing the build are true, and three of them were not.

#### What was found, in order of how much it changes

**1. There is no library object, and `npkc` exit 0 is not the check it looked
like — RX-115.** All eight files in `src/` compile at `npkc` exit 0. All eight
are refused by `llc`: `use of undefined value '@npk_failsafe'`. Counted rather
than inferred — `npkc` emits **seven call sites** to that symbol into every
translation unit and **no `declare` for it, ever**; a program is saved only by
its own `failsafe` producing a `define`, and a library file has nothing to
produce one and under D-248 may not. So the shape the language mandates for a
library is the shape whose IR cannot be assembled, and `npkc`'s usage line
offers no library mode. `BUILD.md` §2's pipeline could not run as drawn.

*The route is worth recording.* Acceptance item 1 said "`npkc src/lib.npk`
succeeds and emits IR". It does. Had the subcycle stopped there — as the
acceptance literally permitted — it would have closed green over a library that
cannot be built, which is precisely what registry O-N11 exists to warn about.
The four-step recipe was run because 0.0.0's dispatch insisted on it, and that
is the only reason this was found.

**2. Two of the three ways to write the umbrella do not work — RX-113.** P-7's
premise (`use` is not transitive) is correct and its mechanism was unverified.
A plain `use` re-exports nothing. A **plain `use` above a `pub use` of the same
path silently cancels the re-export**: `symtab_bind_import` declines a name
already bound and, on the same-declaration path, returns the prior binding
**without merging `SYM_PUB`**. The file compiles at exit 0, nothing is reported
at any severity, and the failure surfaces in the *consumer* as "cannot find
`ERegexPattern` in this scope". Transcript §E2 and §E3 are the same two lines in
the two orders: different behaviour, identical output. It cost this subcycle its
first experiment.

**3. `check_no_syscalls` could not have worked as specified — RX-116.** The
consumer's object has **29** undefined symbols including `npk_open`, `npk_read`,
`npk_write` and `npk_sys6`, and it contains no library code at all: they are the
prelude's, and `opt -O2` removes one of the twenty-nine. An absolute allowlist
would fail on an empty program. With no library-only object to scan (finding 1),
the check becomes a **difference against a committed baseline**, measured here:
baseline 29, consumer 29, symmetric difference empty. RX-008's rule is
untouched; only its enforcement moved.

**4. The probe suite cannot be declared in one manifest entry — RX-119.** 16 of
23 probes carry `expect-exit:` and 7 carry `expect-error:`. Read out of
`npkg/suites.npk` rather than assumed: `run_program` (:779) does not skip a file
with `expect-error`, and `run_compile` (:608) does not filter by expectation
either — `kind` selects the checker, not the file set. The split is 0.0.2's,
because 0.0.2 builds the runner and because the paths are cited by a verified
record.

**5. `accept` was the wrong stage — RX-117.** It is judged by the compiler's
`tools/check`, which RX-007 forbids importing, and it neither links nor runs.
`compile`/`positive` does both.

#### The corrections that arrived mid-task, and what they cost

Three arrived from the coordinator while the work was in progress. **Two stand
and one was itself reversed**, which is the part worth recording.

- **`buffer` is not bounds-checked — RX-118, and it stands.** Verified here
  before it was acted on: `buffer_bytes` is on `TYPE_REFERENCE.md` §23's
  *"deliberately NOT landed"* list, so there is no slice route and every byte
  goes through `buf.ptr[i]`, a `uint8->`. S-23 gains a fourth row and **`Bytes`
  gains the accessor pair `Vec` already owed** — which matters because every
  replacement this library performs is composed into a `Bytes`, from bytes a
  caller controls. It was the one structure in `src/core/` the rule did not
  reach.
- **The live escape rule is parameter-rooted, and it stands.**
  `borrows_only_param_rooted` (`escape.npk:507`, called at :425 before
  `BORROW_RETURNED`). The **pointer-shaped root** formulation is DEF-3's
  *future* rule. Both are now labelled as which they are, in RX-112, in O-N9's
  entry and in probe07's header, because quoting the wrong one as current is how
  a later cycle builds on a rule that is not in force yet.
- **`NITPICK-BORROW-012` was declared non-existent, then restored.** The first
  correction observed — correctly — that the code appears nowhere in the pinned
  toolchain; the conclusion drawn from that was wrong. It is allocated by DEF-3
  **step 2** and lives only in an unlanded worktree commit. All four sites now
  say the thing neither earlier version did: **the code cannot be reproduced
  against `950bb1d`, and its absence there is expected rather than evidence.**

**The general lesson, and it is new to this ecosystem's documents:** *a fact
about an unlanded compiler commit cannot be checked against the pin, and
"I grepped the pinned tree and it is not there" is not a refutation.* Two
sessions reached the same wrong conclusion from the same correct observation
within an hour. Where a claim concerns work in flight, the pin can only ever
confirm absence, so the source has to be named and the claim marked
unverifiable-until-repin — which is what those four sites now do.

#### Language and tooling facts this subcycle established

- **A `failsafe` may name an error that cannot reach it.** No unreachable-arm
  diagnostic (§E4). So a consumer can state a library's contract in its handler
  before the library has any callable surface — which is what makes
  `import.npk` meaningful today.
- **`(*)` and the named arms are two different rules.** §E5 omits the wildcard
  and gets `NITPICK-PICK-003` (exhaustiveness); §E6 omits one system arm and
  gets `NITPICK-REACH-002`, whose text says *"`(*)` counts for nothing here"*.
- **Several single-name `pub use` lines from one path compose**, in either
  order. So P-7's one-name-per-line surface is buildable.
- **A module name is an identifier and all eight chosen here are free.** Checked
  against `src/frontend/keywords.npk` — the generated table, not the prose — with
  `stack`, `range`, `end`, `limit`, `buffer` and fifteen others as controls to
  prove the extraction was reading the right thing.
- **`npkrt.o` defines 201 symbols**, `__divti3`, `memcpy` and `memset` among
  them, so the closed-world link needs no compiler-rt beside it.
- **A phase-ordering trap in reading diagnostics.** `npkc` stops after resolve,
  so a file with both an unresolved name and a type error reports only the
  first. An early reading of the re-export matrix concluded that `use` *was*
  transitive for types, because the type error was never reached. Every
  subsequent matrix used one consumer per symbol kind. *This is the same shape
  as the playbook's warning that a file which fails early looks fast.*

#### What it cost

**Claimed 23:15, three commits at 23:43, 23:47 and 23:51, and the record after
— call it an hour and a quarter of wall clock, measured from the board's claim
line and `git log`, not estimated.** Roughly half of it went on the two findings
above; the skeleton itself is twenty minutes of typing. The single largest item
was RX-113, and not because the fix was hard: the first `src/lib.npk` written
here was the *broken* shape, and the diagnostic pointed at a consumer file that
was correct.

*Recorded because the last record's number was wrong in this exact way:* 0.0.0
reported "~9 h" for six commits spanning 34 minutes, and W-4's rebalancing reads
that number directly. If a later session finds this one's estimate wrong, the
commit timestamps are the evidence and they are above.

#### A note for whoever writes 0.0.2

Three things are waiting, all of them written down rather than remembered:

1. **The probe suite's split** (RX-119) — the three-entry shape is in
   `nitpick.toml` ready to uncomment, and the checklist item is in the cycle
   `README.md`. Moving the seven refusal probes also moves paths cited by
   0.0.0's verified record; say so there when you do it.
2. **`check_no_syscalls` is differential now** (RX-116). The baseline program is
   an empty `main` and a `failsafe`, and its undefined-symbol set is committed.
3. **The build goes through a program root, not over `src/`** (RX-115). There is
   no `build/nregex.o` to produce, and `npkc src/lib.npk` is a parse-and-resolve
   check and nothing more. The harness must not read its exit code as a build.

And one thing that is *not* waiting: `check_layering` at 0.0.3 gains a two-line
check over the `use`-edge list it already builds — **no file contains both a
plain `use` and a `pub use` of the same path** (RX-113). It is the only
mechanical defence against a silent failure mode.

---

### The REPORT block

```
REPORT nitpick-regex 0.0.1
status: DONE
stream: s1
model: claude-opus-5
toolchain: 950bb1d
commits:
  - d84b185 cycle 0.0.1: the module skeleton, and what building it found
  - 590029c cycle 0.0.1: the manifest's first test entry, and why the second is a comment
  - bd0c821 cycle 0.0.1: CI, the harness stub, and the two guides re-read against the verdicts
  - HEAD cycle 0.0.1: close — the checklists, the record, and the report
harness: not run: no harness until 0.0.2 (harness/run.py is a toolchain-pin stub)
  npkc src/core/core.npk -> exit 0        ; llc on its IR -> exit 1 (undefined @npk_failsafe)
  npkc src/unicode/unicode.npk -> exit 0  ; llc -> exit 1 (same)
  npkc src/syntax/syntax.npk -> exit 0    ; llc -> exit 1 (same)
  npkc src/hir/hir.npk -> exit 0          ; llc -> exit 1 (same)
  npkc src/compile/compile.npk -> exit 0  ; llc -> exit 1 (same)
  npkc src/engine/engine.npk -> exit 0    ; llc -> exit 1 (same)
  npkc src/api/api.npk -> exit 0          ; llc -> exit 1 (same)
  npkc src/lib.npk -> exit 0              ; llc -> exit 1 (same)   [RX-115]
  4-step tests/conformance/import.npk -> npkc 0, llc 0, ld 0, run 0
  opt -O2 leg tests/conformance/import.npk -> opt 0, llc 0, ld 0, run 0
  llvm-nm --undefined-only import.o -> exit 0, 29 symbols; baseline 29; difference empty
  control E1 plain `use` in the umbrella -> npkc 1 (NITPICK-RESOLVE-002, in the consumer)
  control E2 plain `use` above `pub use`, same path -> lib npkc 0 SILENT; consumer npkc 1
  control E3 the same two lines reversed -> npkc 0
  control E4 consumer without the (ERegexPattern) arm -> npkc 0
  control E5 consumer without (*) -> npkc 1 (NITPICK-PICK-003)
  control E6 consumer without one system arm -> npkc 1 (NITPICK-REACH-002)
  4-step tests/probe/probe06b_subview_returned.npk -> npkc 0, llc 0, ld 0, run 0
  4-step tests/probe/probe07_string_bytes_edges.npk -> npkc 0, llc 0, ld 0, run 0
  python3 harness/run.py (pinned toolchain) -> exit 0
  python3 harness/run.py (NPKC=/nonexistent) -> exit 1
check: clean
record: meta/roadmap/0.0/0.0.1.md — DONE (2026-09-03)
next: n/a
findings-for-playbook:
  - A FACT ABOUT AN UNLANDED COMPILER COMMIT CANNOT BE CHECKED AGAINST THE PIN, and "I grepped the pinned tree and it is not there" is not a refutation. NITPICK-BORROW-012 was declared non-existent on that evidence and then restored: it is allocated by DEF-3 step 2 and lives only in an unlanded worktree. Two sessions drew the same wrong conclusion from the same correct observation within an hour. Name the source and mark the claim unverifiable-until-repin.
  - THERE IS NO LIBRARY OBJECT. npkc emits calls to @npk_failsafe into every translation unit and never a `declare`, so any module that is not a program root compiles at npkc exit 0 and is refused by llc. A library reaches the compiler only by being imported from a program root, and BUILD_REFERENCE §4.1's "each module compiles to its own object" is not achievable at 950bb1d. Every library in this ecosystem has this, not just this one. (O-N14 provisional.)
  - A `pub use` IS SILENTLY DOWNGRADED TO A PLAIN `use` when the same path was plain-`use`d earlier in the file. symtab_bind_import declines a name already bound and returns the prior binding without merging SYM_PUB. No diagnostic at any severity; the failure appears in the consumer as "cannot find X in this scope"; the two lines in the other order are correct. Every library with an umbrella module is one redundant `use` from it. (O-N13 provisional.)
  - `pub use "p".*;` and `pub use "p".{a,b};` and several single-name `pub use "p".Name;` lines all re-export, in either order, and a plain `use` re-exports nothing — for a type, a function and an `error:` identity alike.
  - A `buffer` IS NOT BOUNDS-CHECKED. `buffer_bytes` is on TYPE_REFERENCE §23's "deliberately NOT landed" list, so there is no slice route: every byte is `buf.ptr[i]`, a `uint8->`. Any byte sink over a `buffer` is unchecked exactly as a `Vec<T>` is. This enlarges the playbook's D-070 row, which currently names only the pointer case.
  - THE LIVE ESCAPE RULE IS PARAMETER-ROOTED: `borrows_only_param_rooted` (escape.npk:507, called at :425 before BORROW_RETURNED). The "pointer-shaped root" formulation circulating in this ecosystem is DEF-3's FUTURE rule. Say which you mean and cite the line.
  - A `failsafe` MAY NAME AN ERROR THAT CANNOT REACH IT — there is no unreachable-arm diagnostic. So a consumer can state a library's contract before the library has any callable surface.
  - npkc STOPS AFTER RESOLVE, so a file with an unresolved name and a type error reports only the first. A matrix that varies two things at once will attribute the wrong cause; use one consumer per symbol kind. Same shape as "a file that fails early looks fast".
  - AN UNDEFINED-SYMBOL ALLOWLIST CANNOT EXPRESS "no syscalls". A program containing no library code has 29 undefined symbols including npk_open/npk_read/npk_write/npk_sys6 — the prelude's — and opt -O2 removes one of them. The check has to be differential against a committed empty-program baseline.
  - A `[[test]]` ENTRY'S `path` IS A DIRECTORY, NEVER A FILE (npkg/suites.npk files_of:549), `recursive` defaults false, `run_program` does not skip a file carrying `expect-error`, and `run_compile`'s `kind` selects the checker and not the file set. A directory mixing positive and negative cases is not declarable; the compiler's own answer is a directory per kind.
open-questions-raised: O-N13, O-N14 (both PROVISIONAL — next free registry numbers proposed, awaiting confirmation)
for-the-author:
  - TWO COMPILER FINDINGS, NEITHER WORKED AROUND, NEITHER A STOP. O-N13 (the silent `pub use` downgrade) and O-N14 (no library module can be assembled). W-27: O-N14 BLOCKS a per-module object, a libnregex.o, and separate compilation as BUILD_REFERENCE §4.1 describes it; it INCONVENIENCES 0.0.2's harness, which builds through a program root, and RX-008's scan, which becomes differential; it TOUCHES nothing else — no rule, no API, no layering, and nothing was reshaped to dodge it. O-N13 BLOCKS nothing, INCONVENIENCES once and expensively per person who meets it, and TOUCHES nothing that compiles. Recommend filing both; O-N14 is close kin to O-N11/DEF-5 and one emitted `declare i32 @npk_failsafe(i32)` closes it outright and improves DEF-5's own case.
  - THE O-N RENUMBERING COULD NOT USE THE RECOMMENDED `O-C` PREFIX. O-C1 and O-C2 are already this repository's compilation questions, cited in five files, so renumbering the legacy four onto it would have recreated the collision it was meant to remove. The prefix is `O-G` ("a Gap in the compiler"), one-for-one and in order, with a row added to the prefix table. Confirm, or name a different letter and it is one sed.
  - THE WORKBENCH REGISTRY NEEDS ONE EDIT I CANNOT MAKE (W-16). Its `O-N2` entry lists this repository's local id as `nitpick-regex O-N3`; it is now `O-G3`.
  - `meta/roadmap/0.0/0.0.0.md` WAS NOT RENUMBERED, deliberately — it is a verified artifact, and renumbering would make it say what was not true on the day. Two redirect entries keep its citations resolving. The ONE change made to it is the "Five commits" -> "Six commits" correction you directed, marked in place. Confirm that boundary is where you want it.
  - CI IS WRITTEN AND HAS NOT RUN. It needs a push, which is not mine, and its one unrehearsable step is building the compiler, which W-18 refuses from here. The acceptance box is struck rather than ticked. Everything checkable was checked: YAML and triggers, the pinned commit's presence on the public remote, the LLVM asset name against the release API, quickemit.py's output paths read from its source at the pin, and the stub run green and red.
  - `probe` IS NOT DECLARED IN THE MANIFEST (RX-119) and the acceptance item is marked half-done. Splitting tests/probe/ by kind means moving seven files whose paths cycle 0.0.0's verified record cites. I judged that beyond this subcycle's remit; if you would rather it happened now, it is seven `git mv`s and a marked note in the record.
compiler-defect: tests/conformance/TRANSCRIPT.txt §A and §E2 — the reproductions for O-N14 and O-N13. Neither stopped the stream: neither was worked around and neither blocks 0.0.1's deliverables.
tokens-and-time: unknown for tokens — no harness reports either until 0.0.2. Wall clock: claimed 23:15, commits at 23:43, 23:47, 23:51 and the close after, so roughly an hour and a quarter, measured from BOARD.md's claim line and `git log` rather than estimated. Roughly half went on RX-113 and RX-115; the skeleton itself is twenty minutes of typing. Peak memory of any command: unmeasured, and nothing here is large — the biggest artefact is 421 KB of IR from an empty library.
```

---

### 2026-09-04, stream 1, toolchain `950bb1d` — the re-dispatch

A verifier FAIL on one number, and one acceptance box that became tickable
overnight. **0.0.1's substance was confirmed in full** before this entry was
written: both mechanical gates, the whole official harness line, and both
compiler findings, including O-N14's reach — the verifier attacked the claim
that it is *every* library rather than this one, and could not narrow it.

**1. The FAIL: `$NPKC --help` exits 2, and the transcript said 1.**

`tests/conformance/TRANSCRIPT.txt` §A2 was committed with `-> exit 1`. Re-run
here rather than taken from anyone's number — the whole discipline this is
about — twice, against the same `npkc`, sha256 matched against `SHA256SUMS`
before either run:

```
$ $NPKC --help
unknown argument: --help
usage: npkc <root.npk> [-o out.ll] [--obligations DIR] [--elide nitpick.obligations] [--extra-picky=no-wildx]
-> exit 2
```

**The number was the only thing wrong.** The two output lines are byte-identical
to what the binary prints — compared with `cat -A`, not by eye — and §A2's
substantive point is untouched: `npkc`'s usage line offers no library or module
mode, which is half of O-N14's evidence. `--help` is simply not a recognised
flag. **Swept for other citations** of the number, twice with different
phrasings and again case-insensitively, and checked the list against a member
already known to belong: there are none. The three prose sites that lean on this
block — `DECISIONS.md`'s RX-115, `OPEN_QUESTIONS.md`'s O-N14 and §1 of this file
— quote the *usage line* and never the exit code, so none needed a word changed.
The correction is **dated in the block and in the file's header** rather than
made silently: a file that claims to be verbatim has to show where it was
touched after the fact.

**What the FAIL was actually worth — transcript §F is new.** The reason a wrong
exit code could survive a commit is that **nothing anywhere said what `npkc`'s
exit codes mean**, while `BUILD.md` rule B-6 instructs the harness to assert on
exactly those integers. Read out of the compiler's own `src/main.npk` at the
pin rather than inferred: **0** success; **1** the program was refused, with
diagnostics on stderr; **2** the driver could not proceed and judged nothing at
all — unknown argument, a second root, no root, a flag twice, an unwritable
output path; **3** a failsafe trap. **2 is not a refusal.** A rejection test in
0.0.2 that expects 1 and gets 2 has not been refused, it has not been
*compiled*; a harness reading "nonzero" as "refused" would score its own
mistyped command line as a conformance result. The compiler intends the split
rather than tolerating it — its own 1.5.0 plan, P-20, specifies "an unknown
argument exiting 2 with the usage".

**And §F's own list was short by two on its first writing**, which is worth more
than the embarrassment. The exit-2 sites `:89` (a flag given without its
argument) and **`:157`** were both missed until the list was checked back
against the source line by line — the dispatch's warning that *every sweep list
in this ecosystem has been short by one on its first telling* held again, at a
count of two. A cited line number was also wrong in the first draft of §F
(`:176` for `:177`) and was caught the same way — by reading the source rather
than trusting the note.

> **CORRECTED 2026-09-04, at the 01:10 re-dispatch.** The paragraph above
> continued: *"`:157` is the one that matters: `front_run` passes 2 up when
> `graph_load_all` returns < 0 …, so **a `use` naming a path that is not there
> exits 2, not 1**. This library imports by relative path … so a typo'd or moved
> import is exactly that case, and a harness scoring nonzero as 'refused' would
> report a missing file as a passing rejection test."* **The causal claim was
> false and is struck.** A missing import exits **1** with
> `NITPICK-RESOLVE-005`; only a missing **root argument** reaches `pass 2i32`.
> How the correction was obtained: a verifier measured it three times, the
> orchestrator twice more, and this session three times again on a byte-faithful
> copy of this repository's own `tests/conformance/import.npk` with only the
> `use` path typo'd — eight runs across three sessions, identical every time —
> and only then was the mechanism re-read at the pin. Both measurements are now transcript
> **§G**, and §F carries the same dated note. **The enumeration is not struck** —
> `:157` is a real exit-2 site and the count of fifteen was independently matched
> site for site; what was wrong was what reaches it.

**2. CI has run, and it is green — the struck box is now ticked.**

Run
[`33835762747`](https://github.com/alternative-intelligence-cp/nitpick-regex/actions/runs/33835762747),
conclusion **`success`**, job `build` green, **8m48s**, on a `push` of `main` at
**`c7b8711`** — 0.0.1's own close commit, so what went green is exactly the tree
this subcycle closed with, not a later one. Read back off the run with `gh`
rather than accepted from the dispatch that reported it; all sixteen step
records are `success`, of which ten are this workflow's declared steps and six
are the runner's own setup, cache-post and teardown.

**The two steps that could not be rehearsed from here both ran.** *Build the
compiler from the pinned commit* is `success` and not `skipped` — the cache was
cold on a first run, so the build genuinely happened, through the compiler's own
`quickemit.py`. *Put LLVM on PATH and assert the pin* **asserts** 20.1.2 and
exits 1 on any other patch release rather than reporting it. That is precisely
the step W-18 forbids rehearsing locally, and the reason this box was struck on
the day rather than ticked.

**Stated plainly, because a green tick invites over-reading.** The run proves
the pin is real: the compiler builds from a clean checkout on a machine that is
not this one, with no prebuilt binary anywhere in the path. It proves 0.0.1's
one substantive acceptance reproduces there — the step *The conformance
consumer, all four steps* compiles, links, **runs** `import.npk` and judges the
binary's exit code, which is why the workflow is not green over nothing. It
proves **nothing about this library's behaviour**, because there is none yet:
`harness/run.py` is the toolchain-pin stub until 0.0.2, so **no test suite ran
and no stage was exercised**, and the manifest's `probe` entry is still
commented out. `ci.yml`'s header said this already; one sentence of it has been
sharpened, because it read as though the *whole run* were no evidence about this
library, which understates the last step.

**3. One annotation from the run — a line, not a fix.**

> Node.js 20 is deprecated. The following actions target Node.js 20 but are
> being forced to run on Node.js 24: `actions/cache@v4`, `actions/checkout@v4`.

Nothing is broken, the run is green, and there is nothing to do today. Recorded
in `ci.yml`'s header, next to the pins, because that is where the CI design
lives. It stops being a warning and becomes a failure when the forcing ends, and
the fix is to bump both actions to a release built for Node 24. **It is worth a
line for a reason beyond housekeeping:** `@v4` is a moving tag, so those two
actions are **the only inputs in that file not pinned to something immutable**.
A workflow this insistent about pinning a compiler by sha and LLVM by exact
patch release should not be surprised when the parts it left on a floating tag
roll forward underneath it.

**4. What came back confirmed, recorded so it is not re-litigated.**

- The two compiler findings are **filed**: **O-N14** and **O-N13**, both numbers
  confirmed against the workbench registry, with the W-27 analysis carried over
  intact.
- The **`O-G` prefix is confirmed.** The recommended `O-C` would have recreated
  the collision it was meant to remove, because `O-C1` and `O-C2` are already
  this repository's compilation questions.
- **`0.0.0.md` is not renumbered**, confirmed: it is a verified artifact, and the
  redirect entries are the right answer.
- The workbench registry edit I could not make under W-16 **has been made** —
  `O-N2`'s entry now names this repository's local id as `O-G3`. Verified by
  reading it, not assumed.

**What it cost.** About three quarters of an hour, almost all of it verification
rather than editing: the two `--help` re-runs and a third to confirm the
committed text, the sweep in three phrasings, reading the compiler's driver and
`pipeline.npk` to establish the exit alphabet from source, checking that list
back against the source line by line — which is what caught its own two
omissions — and pulling the run's steps and annotations back off GitHub. Four
commits. Nothing here compiled
anything large; the biggest command was a `gh api` call.

---

### 2026-09-04 01:10, stream 1, toolchain `950bb1d` — the second re-dispatch

A verifier FAIL on **one causal claim**, and nothing else in 0.0.1 re-opened.
The claim was mine, written in the pass above; the orchestrator had already
propagated it into the workbench playbook, so it was travelling.

**What was confirmed, and is not in question.** The exit alphabet, the
enumeration of fifteen `exit 2` sites, exit-0 at two sites and exit-1 at three,
and `3` being real rather than inferred — the verifier counted independently and
matched §F **site for site**. §F's list is right. Its `2` entry is now
*fifteen sites in all*, said out loud, so the next reader can check the count
without re-deriving it.

**The FAIL: "a `use` naming a path that is not there exits 2, not 1" is false.**
A missing import exits **1**, with `NITPICK-RESOLVE-005: cannot find …`.

**Measured before anything was written.** Two scratch trees, each with a copy of
this repository's `src/` and one copy of `tests/conformance/import.npk`; the
control copy sha256-matched byte-for-byte against the committed file, the other
differing from it in exactly one line — the `use` path. The control compiles at
exit 0, so the only variable is the path. The typo'd copy gives exit 1 and
`RESOLVE-005`, twice. A root argument naming a file that is not there gives
exit 2 with **0 bytes on stdout and 0 on stderr**, counted with `wc -c`. All of
it is transcript **§G**, which is new: §F said of itself *"this is not a
measurement, it is a reading"*, and a reading was exactly the problem, so the
measurements got their own section rather than being folded into a reading.

**Only then the mechanism, re-read at the pin.** `graph_load_all`
(`module_graph.npk`:440) returns < 0 **only** from its own direct `graph_load`
on the command-line root (:441-:444); its walk over discovered imports is a
`drop`ped, NIL-returning `graph_load_imports` (:457), and it ends `pass first`
(:460) — still the root's result. A transitive `use` goes through
`graph_load_import` (:498), where `resolve_use` does its **own** existence check
(:507) and on not-found pushes a diagnostic and returns (:508-:511) **without
ever calling `graph_load`**. `report_resolution` (`resolve_path.npk`:220-:223)
pushes `RESOLVE_NO_SUCH_FILE`, whose string is `"NITPICK-RESOLVE-005"`
(`resolve_codes.npk`:19). The diagnostic then meets the ordinary
"diagnostics exist" gate at `pipeline.npk`:250 — `pass 1i32` — and
`main.npk`:158-:160 makes it the ordinary refusal. Measured output and source
agree, which is the only reason the mechanism is worth writing down at all.

**THE CORRECTED HAZARD IS SHARPER THAN THE ONE I WROTE**, which is why this was
a re-dispatch and not a deletion. A missing import produces exit **1** — *the
very code a rejection fixture expects*. A rejection test whose fixture path is
typo'd, or whose file is later moved, therefore **passes for the wrong reason**:
it wanted a refusal, it got a refusal, and the refusal was about the path rather
than about the thing under test. Nothing reports it. So **a rejection fixture
must assert the diagnostic CODE as well as the exit code** — exit 1 alone cannot
tell "refused for the reason this test is about" from "the file was not there" —
and this library imports by relative path until O-G3 closes, so moved and
mistyped paths are the ordinary case here.

**`BUILD.md` already had the rule that closes this**, which is the part worth
keeping. Rule **B-7** (D-237) says the set of codes a rejection test reports must
*equal* the set its expectations name. The false claim's real cost was never the
wrong integer: it was that it pointed 0.0.2's harness author at the exit code
alone, in the one place where B-7 is what makes the trap unreachable. §F now
says so, addressed to 0.0.2.

**The sweep, and it was short again — for the fifth time, in a new way.** The
dispatch named four sites. Run in five phrasings with `-i`, the tree holds
**three**: transcript §F, the narrative above, and the **second** REPORT block's
`findings-for-playbook`. The **first** REPORT block does not carry it — checked
line by line rather than assumed from the dispatch, and `0.0.0.md`'s block does
not carry it either. *A dispatch's list of sites is a claim like any other.*

**One site cannot be corrected, and saying so is the correction.** Commit
`e478a6a`'s subject is *"the exit-2 list was short by two, and one of them bites
libraries"* — "one of them bites libraries" **is** the false claim, in six words.
It is pushed, CI run `33835762747` ran on the tree that followed it, and history
is not rewritten for a wrong subject line. The commits list in the REPORT block
below quotes it verbatim, because it quotes history. This paragraph is the
annotation a reader needs.

**What it cost.** About half an hour, and the shape of the spend is the finding:
**the measurements took under a minute** — two four-line runs — while the sweep
and the writing took the rest. §F was argued from source for an hour in the pass
above and was wrong; it was settled by running the binary in sixty seconds. Two
commits.

**The general lesson, and it is not only mine.** *Confirming that a code path
EXISTS is not confirming that your scenario REACHES it.* Three failures in this
ecosystem in one night were that same shape — a diagnostic grepped for in the
wrong tree, line numbers taken from the wrong tree, and a `pass 2i32` correctly
found and wrongly reached. Each time the observation was right and the inference
was not. **Where the behaviour can be produced, produce it**: §F is a reading and
§G is a measurement, and only one of them was ever wrong.

---

### The REPORT block

```
REPORT nitpick-regex 0.0.1
status: DONE
stream: s1
model: claude-opus-5
toolchain: 950bb1d
commits:
  - d84b185 cycle 0.0.1: the module skeleton, and what building it found
  - 590029c cycle 0.0.1: the manifest's first test entry, and why the second is a comment
  - bd0c821 cycle 0.0.1: CI, the harness stub, and the two guides re-read against the verdicts
  - c7b8711 cycle 0.0.1: close — the checklists, the record, and the report
  - 35cdc38 cycle 0.0.1: the --help exit code was 2, not 1, and why that distinction matters
  - 613c4c2 cycle 0.0.1: CI is green at 33835762747, and the Node 20 annotation
  - e478a6a cycle 0.0.1: the exit-2 list was short by two, and one of them bites libraries
  - 9c81bc9 cycle 0.0.1: the record's commit list, and the re-dispatch closed
  - HEAD cycle 0.0.1: a missing import exits 1, not 2 — the causal claim corrected and measured
harness: not run: no harness until 0.0.2 (harness/run.py is a toolchain-pin stub)
  $NPKC --help -> exit 2   (run twice, sha256 matched first; was committed as 1 — the FAIL)
  $NPKC --help -> exit 2   (second run, same)
  $NPKC tests/conformance/import.npk -o $SP/ok.ll   (byte-identical copy, CONTROL) -> exit 0
  $NPKC tests/conformance/import.npk -o $SP/typo.ll   (same file, ONLY the use path typo'd) -> exit 1, NITPICK-RESOLVE-005 import.npk:32:1: cannot find `../../src/lib_MISSING.npk`
  $NPKC tests/conformance/import.npk -o $SP/typo2.ll  (second run, same) -> exit 1, same diagnostic
  $NPKC no_such_root.npk -o $SP/nsr.ll   (root argument names no file) -> exit 2, silent
  $NPKC no_such_root.npk                 (no -o) -> exit 2; stdout 0 bytes, stderr 0 bytes by wc -c
  sha256sum npkc/npkrt.o vs SHA256SUMS -> exit 0, both matched before the runs above
  git show 950bb1d:src/frontend/module_graph.npk / resolve_path.npk / resolve_codes.npk (read-only, W-18) -> exit 0
  gh run view 33835762747 --json conclusion,headSha -> exit 0; success, c7b8711dd…
  gh api .../runs/33835762747/jobs -> exit 0; 16 step records, 16 success, job `build` success
  gh api .../check-runs/100907841484/annotations -> exit 0; 1 warning, Node 20 deprecation
  python3 -c yaml.safe_load(ci.yml) -> exit 0; 1 job, 10 declared steps
  git show 950bb1d:src/main.npk / src/driver/pipeline.npk (read-only, W-18 respected) -> exit 0
  python3 check_refs.py -> exit 0, clean
  python3 check_record.py nitpick-regex 0.0.1 -> exit 0, record clean
check: clean
record: meta/roadmap/0.0/0.0.1.md — DONE (2026-09-03, amended twice on 2026-09-04)
next: n/a
findings-for-playbook:
  - NOBODY HAD WRITTEN DOWN WHAT npkc's EXIT CODES MEAN, and rule B-6 tells every harness in this ecosystem to assert on exactly those integers. Read out of src/main.npk at 950bb1d: 0 success; 1 REFUSED, with diagnostics; 2 the driver could not proceed and judged nothing (unknown argument, a second root, no root, a flag twice, an unwritable path); 3 a failsafe trap. 2 IS NOT A REFUSAL. A rejection test expecting 1 that gets 2 was never compiled, and a harness reading "nonzero" as "refused" scores its own mistyped command line as a conformance result. Every library here is about to write such a harness.
  - THE SWEEP RULE APPLIES TO A LIST YOU WRITE, not only to a list you inherit. §F's own enumeration of npkc's exit-2 sites was short by TWO on its first writing (:89 and :157), and one cited line number was off by one, both caught only by re-reading the compiler source line by line. The dispatch's "every sweep list in this ecosystem has been short by one on its first telling" now holds four for four, and the fourth was a list written BY the session that was quoting the rule.
  - CORRECTED 2026-09-04 at the 01:10 re-dispatch; this line first read "A `use` NAMING A PATH THAT IS NOT THERE EXITS 2, NOT 1" and that was FALSE. A MISSING IMPORT EXITS 1, WITH NITPICK-RESOLVE-005 — the very code a rejection fixture expects — so a rejection test whose fixture path is typo'd or later moved PASSES FOR THE WRONG REASON, and nothing reports it. Only a missing ROOT ARGUMENT reaches pass 2i32, silently: graph_load_all's return is fed solely by its own direct call on the command-line file, while every `use` goes through resolve_use, which does its own existence check, pushes a diagnostic and never calls graph_load. THE RULE: a rejection fixture must assert the DIAGNOSTIC CODE as well as the exit code, because exit 1 alone cannot tell "refused for the reason this test is about" from "the file was not there". Measured, transcript §G.
  - CONFIRMING THAT A CODE PATH EXISTS IS NOT CONFIRMING THAT YOUR SCENARIO REACHES IT. `pass 2i32` was found correctly and reached wrongly; the observation was right and the inference was not. Three failures in this ecosystem in one night were that shape — a diagnostic grepped in the wrong tree, line numbers from the wrong tree, and this. THE COUNTER-RULE IS CHEAPER THAN THE CARE IT REPLACES: where the behaviour can be produced, PRODUCE IT. This claim was argued from compiler source for an hour and was wrong; two four-line runs settled it in under a minute. A section that says of itself "this is not a measurement, it is a reading" is naming its own failure mode.
  - A DISPATCH'S LIST OF SITES IS A CLAIM LIKE ANY OTHER, and it is checked, not consumed. The re-dispatch named four places the false claim had travelled to; swept in five phrasings with -i, the tree held three, and one named site (the FIRST report block) did not carry it at all. Sweep lists in this ecosystem have now been wrong five times running — short four times, and over-long once.
  - A WRONG CLAIM CAN LAND IN A COMMIT SUBJECT, WHERE IT CANNOT BE CORRECTED. e478a6a's subject ends "and one of them bites libraries", which IS the false claim; it is pushed and CI ran on the tree after it, so history stays and the annotation goes in the record instead. Worth a thought before a subject line asserts a finding rather than naming a change.
  - A TRANSCRIPT THAT CLAIMS TO BE VERBATIM MUST SHOW WHERE IT WAS LATER TOUCHED. The fix for a wrong number in a committed transcript is the corrected number PLUS a dated note saying what it was and how the correction was obtained — not a silent edit, which leaves a file asserting it is a faithful record of a run that reported something else.
  - A GREEN CI RUN IS EVIDENCE ABOUT THE TOOLCHAIN LONG BEFORE IT IS EVIDENCE ABOUT THE LIBRARY, and the workflow should say which in its own header. Here the build-the-pinned-compiler and assert-LLVM steps are real evidence from day one, while the harness step is a stub proving nothing — so "CI is green" and "the library works" are months apart, and a checklist that does not distinguish them will be read as the second.
  - THE ONE THING A PINNING WORKFLOW FORGETS TO PIN IS ITS OWN ACTIONS. `actions/checkout@v4` and `actions/cache@v4` are moving tags; in a file that pins a compiler by full sha and LLVM by exact patch release they are the only floating inputs, and they are what produced this run's only annotation.
open-questions-raised: none new (O-N13 and O-N14 filed and confirmed; local O-G3 confirmed in the workbench registry)
for-the-author:
  - `probe` IS STILL NOT DECLARED IN THE MANIFEST (RX-119) and that acceptance item remains marked half-done, deliberately and unchanged by this pass. It is the ONE thing in 0.0.1 that is not finished. Splitting tests/probe/ by kind moves seven files whose paths cycle 0.0.0's verified record cites; the three-entry shape is in nitpick.toml ready to uncomment. Recommend leaving it to 0.0.2, which owns the runner and can move the paths and fix the citations in one commit.
  - THE CORRECTION MUST TRAVEL BACK OUT AS FAR AS THE CLAIM DID. `PLAYBOOK.md` already carries it with a dated note (verified by reading it). `BOARD.md`'s s1 row and `RECORD.md`:1866 still state or quote the old claim, and both are outside my write scope under W-16. No action needed from you if the orchestrator is updating them; flagged only so the last copy is not the one that survives.
  - THE NODE 20 DEPRECATION IS A DATED FUTURE FAILURE, not a defect. Recorded in ci.yml's header. Recommend bumping actions/checkout and actions/cache in the same commit that next bumps the compiler pin, so one deliberate commit runs the full suite for both.
compiler-defect: none new. tests/conformance/TRANSCRIPT.txt §A and §E2 remain the reproductions for O-N14 and O-N13, both now filed; neither was worked around and neither blocks anything 0.0.1 delivered.
tokens-and-time: unknown for tokens — no harness reports either until 0.0.2. Wall clock for THIS re-dispatch (the second): about half an hour, one commit, and the shape of the spend is the finding — the two decisive measurements took under a minute between them, while the five-phrasing sweep and the writing took the rest. The FIRST re-dispatch: about three quarters of an hour, four commits, almost all of it verification rather than editing — the two --help re-runs, a three-phrasing sweep, reading the compiler's driver to establish the exit alphabet from source, and pulling the run's steps and annotations off GitHub. The original pass was an hour and a quarter (claimed 23:15, commits 23:43/23:47/23:51 and the close after), measured from BOARD.md's claim line and `git log`. Peak memory unmeasured and nothing here is large; the biggest command this pass was a `gh api` call.
```
