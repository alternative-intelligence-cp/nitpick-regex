# `meta/research/`

Notes on the algorithms and the conformance suites, and any measurement a
decision cites. `corpora/` holds fetched third-party test suites — gitignored,
with `corpora/README.md` recording which suite, which revision, and how to
fetch it, so a run is reproducible without the bytes living here.
