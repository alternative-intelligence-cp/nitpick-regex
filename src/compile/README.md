# `src/compile/` — the program builder

Codepoint ranges to UTF-8 byte automata, alphabet compression into equivalence
classes, and the HIR to the NFA instruction program. Governed by
`meta/specs/COMPILE.md`. Built in cycles 0.4 and 0.6.
