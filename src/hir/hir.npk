// `src/hir/` — the high-level intermediate. PLACEHOLDER: nothing yet.
//
// FILLED BY cycle 0.2, governed by `meta/specs/HIR.md`. Desugaring and
// normalisation, flattened into a POD arena: a node declares no owning field,
// which is why `HIR.md` H-2 keeps its operands as offsets into a `Bytes` rather
// than as owned strings. That is H-2's design, and a `Vec` of nodes MUST keep it
// (`SAFETY.md` S-23a, RX-155) -- the language would not: an owning element is
// accepted in an array or a `Vec`, and `vec_get` then MOVES it out.
//
// LAYERING (`BUILD.md` §6, rule B-16). May import: `syntax`, `unicode`,
// `core`.
//
// See `src/core/core.npk` for why a placeholder exists at all (P-6).
mod:hir;
